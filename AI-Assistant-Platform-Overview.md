# AI Assistant Platform — 技术能力概览

> 面向 Leadership | 2026-04-03

---

## 一句话定位

**一个可私有部署的企业级 AI Agent 平台**——对标 Manus / Claude Code / OpenClaw，但专为多租户 SaaS 场景设计，让 Wahda、HalalMoney 等业务线通过 SDK + MCP + Skills 即可变身 AI 原生应用。

---

## 核心架构

```
外部 App (Wahda / HalalMoney / ...)
        │  AI Assistant SDK（Python · TypeScript · Swift · Kotlin）
        ▼
┌─────────────────────────────────────────────┐
│          AI Assistant Service (8093)          │
│                                               │
│  ┌─────────┐  ┌──────────┐  ┌────────────┐  │
│  │ 8-Phase  │  │  Skills  │  │  MCP Tool  │  │
│  │ Agent    │  │ Framework│  │  Protocol  │  │
│  │ Loop     │  │          │  │            │  │
│  └────┬─────┘  └────┬─────┘  └─────┬──────┘  │
│       └──────────────┴──────────────┘         │
│  ┌──────────────────────────────────────────┐ │
│  │  25 Models · 5 Providers · 1M Context    │ │
│  │  RAG 检索 · 混合记忆 · 场景路由 · 生图   │ │
│  │  多租户隔离 · 工具审批 · 沙盒执行        │ │
│  └──────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

---

## 技术能力矩阵

| 能力域 | 具体实现 |
|--------|---------|
| **多模型调度** | 25 个模型横跨 OpenAI / Anthropic / Gemini / DeepSeek / 通义千问，自动路由、成本追踪、Vision 支持（13 个视觉模型） |
| **Agent Loop** | 自研 8 阶段流水线：记忆加载 → 场景分析 → 任务规划 → RAG 检索 → 上下文构建 → 工具执行 → 上下文压缩 → 内容生成，全链路流式 SSE |
| **Skills 技能系统** | 动态注册、三种加载模式（builtin / markdown / DB）、正则自动触发、参数 JSON Schema 校验，已有 Wahda 伊斯兰金融 + HalalMoney 理财 mock 技能 |
| **MCP 工具协议** | 标准 JSON-RPC 2.0 实现，支持外部 MCP Server 接入（单服务器最多 50 工具），工具白名单/黑名单、并发控制、安全沙盒 |
| **RAG + 知识库** | 场景感知检索（7 种场景智能路由）、引用追踪、质量评分、向量检索 + 语义打分 |
| **混合记忆** | 三层架构：工作记忆（内存）→ 会话记忆（DB）→ 长期记忆（用户偏好），支持每日反思与记忆合并 |
| **多租户隔离** | 租户级工具策略、速率限制（令牌桶）、审计日志、执行沙盒三级隔离（default / network_restricted / docker_restricted） |
| **AI 生图** | 智能路由 Gemini + DashScope Wanx，支持同步/异步模式 |
| **内容生成** | 自动生成 Word / PPT / PDF、代码执行沙盒、结构化输出校验 |
| **领域策略** | 已实现伊斯兰金融领域策略（80+ 术语库、置信度阈值、阿拉伯语检测），可扩展其他垂直领域 |

---

## 竞品对比

| 维度 | **我们 (AI Assistant)** | **Manus** | **Claude Code** | **OpenClaw** |
|------|----------------------|-----------|-----------------|-------------|
| **定位** | 企业级多租户 AI Agent 平台 | 通用 AI Agent（消费级） | 开发者终端工具 | 开源 Agent 框架 |
| **部署方式** | 私有部署 / 专有云 | SaaS only | 本地 CLI | 自部署 |
| **多模型** | 5 家 25 个模型（含国产通义） | 单一模型 | Claude only | 需自行对接 |
| **多租户** | 原生支持（策略/隔离/审计） | 不支持 | 不支持 | 需自行实现 |
| **MCP 工具** | 原生支持 + 安全沙盒 | 有限支持 | 原生支持 | 需自行集成 |
| **Skills 技能** | 动态注册 + 自动触发 | 不支持 | 基于文件 | 不支持 |
| **领域定制** | 伊斯兰金融策略已实现 | 无 | 无 | 需自行实现 |
| **移动端接入** | SDK + SSE 封装（Swift/Kotlin） | Web only | Terminal only | 无 SDK |
| **知识库 RAG** | 场景感知 + 质量评估 | 基础 RAG | 无内置 | 基础 RAG |
| **合规与审计** | 工具审批、执行沙盒、审计日志 | 无 | 无 | 基础 |

**核心差异化：** 我们不是在做另一个 Manus，而是做一个**可私有部署、多租户、领域定制**的 AI Agent 中台——外部竞品解决的是「个人用 AI」，我们解决的是「企业怎么把 AI 嵌入自己的产品」。

---

## 业务落地路径

### 内部赋能

已具备的能力可直接为内部团队提供：智能文档分析、知识库问答、代码执行、AI 生图、自动报告生成。任何内部系统通过 MCP 协议接入即可被 AI Agent 调用。

### Wahda（语聊 App）集成

```
Wahda iOS/Android App
  └─ AI Assistant SDK (10 行代码初始化)
       ├─ 智能客服：用户提问 → RAG 检索伊斯兰金融知识库 → 流式回答 + 引用来源
       ├─ 合规问答：伊斯兰金融领域策略自动生效（80+ 术语识别 + 置信度校验）
       ├─ 文档分析：上传合同/财报 → 多模态解析 → 结构化摘要
       └─ 自定义 MCP：接入 Wahda 风控系统，AI 可直接查询交易风险
```

### HalalMoney（金融 App）集成

```
HalalMoney App
  └─ AI Assistant SDK
       ├─ 理财助手：消费分析 + 预算规划（Skills 技能自动触发）
       ├─ AI 生图：生成消费分析图表、投资组合可视化
       ├─ 知识问答：清真金融产品 FAQ（RAG + 场景路由）
       └─ 多轮对话：会话记忆 + 用户偏好学习（三层混合记忆）
```

### SDK 接入成本

| 指标 | 数值 |
|------|------|
| 初始化到首条消息 | **< 15 分钟** |
| 初始化代码 | **5 行** |
| 基础聊天（含流式） | **10 行** |
| 完整集成（Skills + MCP + 生图） | **< 50 行** |

---

## 实施节奏

| 阶段 | 周期 | 交付 |
|------|------|------|
| **Phase 1** — SDK 核心 | 4 周 | Python/TS SDK（Chat + Session + KB）、OpenAPI Spec |
| **Phase 2** — 标准能力 | 3 周 | Skills + 生图 + Artifacts、Wahda/HalalMoney 对接 |
| **Phase 3** — 完整生态 | 3 周 | MCP 动态注册、Webhook、性能基准、SDK v2.0 |

**总计 10 周**，Phase 1 结束即可支撑两个 App 的基础 AI 功能上线。

---

## 总结

我们已经构建了一个**技术深度对标 Manus/Claude Code、但定位差异化明显**的 AI Agent 平台。核心壁垒在于：多租户隔离、领域定制能力、MCP + Skills 的可扩展工具生态、以及面向移动端的 SDK 封装。下一步通过 10 周的 SDK 工程化，即可让 Wahda 和 HalalMoney 快速成为 AI 原生应用。
