docker run -d --name my-pg ^
  -e POSTGRES_PASSWORD=111111 ^
  -p 5432:5432 ^
  -v pg-data:/var/lib/postgresql ^
  --restart unless-stopped ^
  postgres:latest

docker run -d --name my-redis ^
  -p 6379:6379 ^
  -v redis-data:/data ^
  --restart unless-stopped ^
  redis:latest redis-server --appendonly yes --requirepass 111111

## 数据库建表

KBMS / 服务注册 / 会话等都依赖 `database/schema.sql` 的表结构。

- 默认会在网关启动时自动检测并初始化（`GATEWAY_DATABASE__AUTO_INIT=true`）。
- 如需手动执行（或排查问题），可运行：`python scripts/init_database.py`
