# General AI Assistant Upgrade Harness Progress Log

**Created:** 2026-06-16

**Harness Folder:** `docs/general_ai_assistant_upgrade`

## Current State

- Status: blocked
- Completed phase: GAA-03
- Active phase: GAA-04
- Active feature-oracle item: GAA-F005
- Clean-state note: harness files were created; repo has pre-existing working-tree changes plus scoped edits from this session.

## Session Log

| Date | Agent Role | Phase | Summary | Evidence | Next Step |
| --- | --- | --- | --- | --- | --- |
| 2026-06-16 | planner/evaluator | GAA-00 | Baseline inspected repo structure, current diff, route map, deployment docs, Makefile, compose files, assistant API, drain behavior, frontend runtime config, and validation commands. | `reports/gaa-00-baseline-release-audit-report.md` | Start GAA-01 assistant core contracts. |
| 2026-06-16 | generator/evaluator | GAA-01 | Implemented artifact schema-missing read contract: metadata, download, and delete lookup now return public 404 instead of leaking DB schema errors. | `reports/gaa-01-assistant-core-contracts-report.md` | Start GAA-02 assistant UI/browser work. |
| 2026-06-16 | generator/evaluator | GAA-02 | Added E2E route coverage for permitted `/assistant` access and model-tester-only redirect to `/playground`; frontend type/build/lint passed. | `reports/gaa-02-assistant-user-experience-report.md` | Unblock browser runtime, run GAA-02 Playwright checks, then decide whether GAA-03 can start. |
| 2026-06-16 | evaluator/runtime unblocker | GAA-02 | Installed Playwright Chromium, fixed Confluence-disabled and API-key legacy schema runtime 500s exposed by the walkthrough, then passed assistant route E2E and full site walkthrough. | `reports/gaa-02-assistant-user-experience-report.md` | Start GAA-03 assistant eval and safety coverage. |
| 2026-06-16 | generator/evaluator | GAA-03 | Added deterministic assistant eval/safety contracts for runtime memory prompt-injection neutralization, PII redaction, tool-output caps, bounded KB summaries, and privacy/refusal guardrails. | `reports/gaa-03-ai-evaluation-and-safety-report.md` | Start GAA-04 deployment readiness validation. |
| 2026-06-16 | evaluator | GAA-04 | Ran non-destructive deployment-readiness gates: static compose, current container health, gateway/frontend health, browser smoke, and service isolation. Formal release validation is blocked by missing root `.env` and provider credentials. | `reports/gaa-04-deployment-readiness-report.md` | Provide root `.env` and provider keys, then re-run GAA-04 without deployment unless explicitly approved. |
| 2026-06-16 | generator/evaluator | GAA-04 | Improved release observability: `make status` works without `.env`, assistant OpenAPI contract passes via in-process dev-only spec generation, and assistant direct host reachability is opt-in for private topology. | `reports/gaa-04-deployment-readiness-report.md` | Provide root `.env`, provider keys, assistant black-box credentials, and deployment approval before final release validation. |
| 2026-06-16 | generator/evaluator | GAA-04 | Removed the hardcoded unavailable assistant default model, added config coverage, and confirmed local black-box chat is blocked because enabled DB models are DashScope/Google while only OpenAI is configured at runtime. | `reports/gaa-04-deployment-readiness-report.md` | Align enabled assistant models with configured provider credentials, then re-run credential-injected black-box isolation tests. |
| 2026-06-16 | generator/evaluator | GAA-04 | Added a `make validate` runtime gate for assistant model/provider alignment and front-end no-model send protection; frontend type-check, lint, build, route E2E, and site walkthrough remain passing. | `reports/gaa-04-deployment-readiness-report.md` | Populate `.env`, align provider/model rows, then run full `make validate` to exercise the new runtime gate. |
| 2026-06-16 | generator/evaluator | GAA-04 | Added automated script-level tests for validate-env quickstart and assistant model/provider alignment: config-only passes, mismatch fails, matching provider passes. | `reports/gaa-04-deployment-readiness-report.md` | Continue GAA-04 hardening until `.env` and provider alignment are supplied. |
| 2026-06-16 | generator/evaluator | GAA-04 | Added Playwright coverage for the assistant no-model state; current-source dev server E2E proves composer/send are disabled and chat stream is not called when model list is empty. | `reports/gaa-04-deployment-readiness-report.md` | Continue GAA-04 hardening or supply `.env` and aligned provider/model rows for full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Fixed release-config drift for chat-specific Vertex env vars: `.env.example`, README, compose injection, and validate-env coverage now agree on `GOOGLE_CHAT_BACKEND` and `VERTEX_CHAT_API_KEY`. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Fixed `deploy-app` app-only scope so application microservice updates restart gateway, frontend, assistant-service, knowledge-service, and MCP docgen instead of only gateway/frontend. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Fixed Makefile deployment targets so documented `ARGS` are forwarded to `scripts/new/deploy.sh`; dry-run evidence confirms deploy options are no longer silently ignored. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Aligned validate-env chat provider inference with `resolve_google("chat")`, including Vertex backend/key combinations, so the model/provider release gate matches assistant-service runtime setup. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Tightened config-only chat key validation so unusable `VERTEX_CHAT_API_KEY` without `GOOGLE_CHAT_BACKEND=vertex` fails before runtime validation. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Added `DOCGEN_ARTIFACT_SIGN_KEY` to release config validation and docs so docgen signed artifact URLs use a stable secret instead of a per-process random fallback. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Scoped infra-only validation to PostgreSQL, Redis, and Qdrant so `deploy-infra` preparation is not blocked by app-only secrets; added regression coverage and service-filtered compose evidence. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Scoped `deploy.sh --pull` to the selected service set so infra-only or app-only pulls do not unexpectedly pull unrelated images. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-16 | reviewer/simplifier | GAA-04 | Added a pre-flight-safe guard that rejects `deploy.sh --infra --app` before Docker checks, env validation, service starts, or migrations. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Made app-only deployments wait for PostgreSQL before default migrations and documented `make deploy-app ARGS="--no-migrate"` for restart-only app deploys. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened automatic migration failure semantics: pending migration detection tolerates no applied row under pipefail, and `migrate.sh --auto` exits nonzero on SQL errors instead of continuing. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened migration SQL file execution so both Docker and local psql paths use `ON_ERROR_STOP=1`; script contract tests now pass 17/17. | `reports/gaa-04-deployment-readiness-report.md` | Supply `.env` and aligned provider/model rows, then run full release validation. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Validated the supplied external env path without reading values; fixed infra-only compose validation so app-only compose vars no longer block infra-only checks. Full release config remains blocked by missing/placeholder required variables. | `reports/gaa-04-deployment-readiness-report.md` | Set non-placeholder `REDIS_PASSWORD`, `DOCGEN_ARTIFACT_SIGN_KEY`, `DEFAULT_USER_PASSWORD`, and `AUTH_ALLOWED_EMAIL_DOMAIN`, then rerun full release validation. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added formal `ENV_FILE=/path/to/.env` support across Makefile validation/deploy/status/migration targets plus deploy/migrate `--env FILE`; dry-runs prove external env paths are forwarded. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun `make validate-config ENV_FILE=...` and `make validate ENV_FILE=...`. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Completed the external-env Makefile coverage for stop/restart/logs, backup/restore/list, and dev helper targets; dry-runs prove they forward `ENV_FILE` without executing operations. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Simplified release-gate failure order: `validate-env.sh` now skips Compose interpolation when required config checks already failed, so the supplied env reports only the four actionable missing/placeholder variables. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened rollback safety for backup restore: `backup.sh` accepts `--env FILE`, refuses missing selected env files before creating `backups/` or accessing the database, and restore uses `psql -v ON_ERROR_STOP=1`; script contract tests now pass 25/25. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; do not restore shared data without a current backup and explicit approval. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened operational argument guards: `backup.sh` and `migrate.sh` now reject unknown options and missing `--env` values before backup, restore, migration status, init, or auto migration actions; script contract tests now pass 27/27. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; typoed operational arguments should fail before action. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Extended env argument guards to release entrypoints: `deploy.sh --env` and `validate-env.sh --env` now reject missing file paths before Docker pre-flight or config/runtime validation; script contract tests now pass 29/29. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; incomplete env-path arguments should fail before action. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Hardened development helper usability: `setup-dev.sh` now parses `--env FILE` before env loading, allows help/status/stop without dev DB/Redis passwords, still requires passwords before start/reset, honors `QDRANT_HTTP_PORT`, and forwards `ENV_FILE` to child migrations; script contract tests now pass 33/33. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; development status/help should remain usable before secrets are complete. |
| 2026-06-17 | reviewer/evaluator | GAA-04 | Repaired phase contracts for the current strict harness validator: all phases now include review evidence and minimal-change scope gates; terminal GAA-04 includes whole-demand regression; harness validator passes with quality score 100. | `reports/gaa-04-deployment-readiness-report.md` | Keep GAA-04 blocked until the supplied env values are complete and full release gates pass. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Aligned `status.sh` with external env operations: it now passes the selected `ENV_FILE` to `docker compose ps` before fallback; script contract tests now pass 34/34 and `make status ENV_FILE=...` still reports all local health checks healthy without printing secrets. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; status should use the same env path as validation and deployment. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened `setup-dev.sh --env FILE`: a missing explicit env file now exits before status/action instead of falling back to defaults; script contract tests now pass 35/35. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; typoed development env paths should fail before action. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Fixed a migration env-path regression found by review: `migrate.sh --env /tmp/missing --status` previously accessed the default database; it now exits before migration status or DB access, and script contract tests pass 36/36. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; migration commands should never fall back after a typoed explicit env path. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened deployment env-path ordering: `deploy.sh --env /tmp/missing --app --no-migrate` now exits before pre-flight instead of entering Docker checks, and script contract tests pass 37/37. | `reports/gaa-04-deployment-readiness-report.md` | Complete the supplied env values, then rerun release gates with `ENV_FILE=...`; deployment commands should fail on typoed env paths before Docker or service actions. |
| 2026-06-17 | reviewer/evaluator | GAA-04 | Expanded authenticated page smoke coverage: `site-walkthrough` now checks visible sidebar routes plus `/knowledge/create` and `/exams`, passing 1/1 with screenshots for 10 stable pages and no console/page/400+ errors. | `reports/gaa-04-deployment-readiness-report.md` | Add seeded-record coverage or a waiver for dynamic ID/share/quiz routes, then rerun full release gates after the supplied env is complete. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Fixed a page-smoke API defect found by dynamic data probing: missing `exams`/`quizzes` tables and absent model registry no longer make read-only exam/quiz list endpoints return 500/503; current stack now returns 200 empty lists and focused API tests pass 4/4. | `reports/gaa-04-deployment-readiness-report.md` | Seed dynamic records or document a waiver for detail/share/quiz-taking routes, then rerun full release gates after the supplied env is complete. |
| 2026-06-17 | reviewer/evaluator | GAA-04 | Added data-backed current-user edit coverage to `site-walkthrough`: it now discovers `/users/:userId/edit` from `/api/v1/auth/me`, passing 1/1 with screenshots for 11 pages. | `reports/gaa-04-deployment-readiness-report.md` | Seed knowledge/exam detail, public share, and quiz-taking records or document waivers before launch. |
| 2026-06-17 | reviewer/evaluator | GAA-04 | Expanded `site-walkthrough` dynamic discovery for knowledge detail, exam detail, public share, and public quiz routes when seed records exist; current read-only probes returned 200 but zero datasets, exams, quizzes, and conversation shares, so the run still covers 11 pages. | `reports/gaa-04-deployment-readiness-report.md` | Create seed records or document waivers for knowledge/exam detail, public share, and quiz-taking route families before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Repaired the non-destructive live-stack failure-isolation breaker check: the sibling route now targets public `/api/v1/health` instead of admin-only `/health/services`; live-stack mode passes 1/1 breaker check. This was later superseded by the compose-backed stop/start row below. | `reports/gaa-04-deployment-readiness-report.md` | Continue with compose-backed failure-isolation evidence and final release blockers. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Repaired compose-backed failure-isolation: the suite now uses the selected compose env file, probes `/assistant/config` instead of hardcoded chat model calls for assistant-service outages, waits for assistant proxy recovery after restart, and passes 4/5 with only absent local `langgraph-agent` skipped. Combined isolation regression passes 8/9 and post-test status remains healthy. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/evaluator | GAA-04 | Added non-mutating dynamic-route render smoke: mocked API responses now prove `/knowledge/:datasetId`, `/exams/:examId`, `/share/:shareId`, and `/quiz/:shareCode` render without console/page/API errors; the spec passes 2/2 and live site walkthrough remains passing 1/1. | `reports/gaa-04-deployment-readiness-report.md` | Still create live seed records or document waivers for those dynamic route journeys before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added a static compose private-port regression: infra/helper service host ports remain bound to `127.0.0.1`, assistant-service stays expose-only on the Docker network, script contract tests pass 38/38, and `.env.example` compose config still parses. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened CORS release validation: full app config now rejects missing CORS arrays, wildcard/non-http origins, and localhost origins once `AUTH_ALLOWED_EMAIL_DOMAIN` is non-local; script contract tests pass 40/40 and external env validation now exposes six release config blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values including explicit CORS origins, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added direct CORS negative regression coverage for wildcard and non-HTTP origins, and made wildcard failures report a specific release-config error; script contract tests now pass 42/42. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values including explicit CORS origins, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened docgen artifact-link readiness: `DOCGEN_PUBLIC_URL` now fails validation when it remains localhost or loopback under a non-local auth domain; script contract tests now pass 43/43 and external env still stops at the same six release config blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, set a browser-reachable docgen public URL for non-local deployments, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened non-local browser-facing URL readiness: CORS origins and `DOCGEN_PUBLIC_URL` now must use HTTPS when `AUTH_ALLOWED_EMAIL_DOMAIN` is non-local; script contract tests now pass 45/45 and external env still stops at the same six release config blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, set HTTPS frontend/docgen URLs for non-local deployments, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added frontend runtime-config entrypoint regression coverage: `runtime-config.js` generation now has safe-default and JS escaping tests; combined script/runtime-config tests pass 47/47 and external env still stops at the same six release config blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, set HTTPS frontend/docgen URLs for non-local deployments, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added nginx no-store delivery for `/runtime-config.js`, so deploy-time frontend env changes are not cached like immutable assets; combined script/runtime-config tests pass 48/48 and external env still stops at the same six release config blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, set HTTPS frontend/docgen URLs for non-local deployments, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened frontend API runtime URL readiness: non-local deployments reject `http://` or localhost/loopback absolute `VITE_API_URL`/`VITE_API_BASE_URL`, while blank defaults and same-origin paths remain valid; combined script/runtime-config tests pass 51/51 and external env still stops at the same six release config blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, use same-origin or HTTPS frontend API runtime URLs for non-local deployments, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Extended frontend browser runtime URL readiness to telemetry: non-local deployments now reject `http://` or localhost/loopback absolute `VITE_TELEMETRY_ENDPOINT`, while blank defaults and same-origin paths remain valid; combined script/runtime-config tests still pass 51/51 and external env still stops at the same six release config blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, use same-origin or HTTPS frontend API/telemetry runtime URLs for non-local deployments, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added frontend auth-domain alignment validation: non-local `VITE_AUTH_EMAIL_DOMAIN` must match `AUTH_ALLOWED_EMAIL_DOMAIN` when explicitly set, so username-only login appends a backend-accepted domain; quickstart tests pass 49/49, combined script/runtime-config tests pass 52/52, and external env still stops at the same six release blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, keep explicit frontend auth domain equal to backend auth domain or omit it for Compose fallback, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added frontend support-email readiness validation: blank `VITE_SUPPORT_EMAIL` now defaults from the active auth domain, explicit `example.com` support mailboxes fail for non-local auth domains, quickstart tests pass 51/51, combined script/runtime-config tests pass 55/55, and external env still stops at the same six release blockers. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, leave support email blank or set a real non-example mailbox for non-local deployments, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Added gateway metrics release smoke: `check_gateway_metrics` verifies `/metrics` reachability and Prometheus markers when metrics exist, `validate-env.sh --runtime` waits for it after readiness, `status.sh` reports it, quickstart tests pass 52/52, and combined script/runtime-config tests pass 56/56. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, scrape gateway `/metrics` in monitoring, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Fixed source-level Prometheus export: a fresh metrics collector now registers request/error/latency/billing/connection/circuit-breaker metrics plus `gateway_up`, histogram labels are preserved, observability tests pass 11/11, and related metrics tests pass 20/20. | `reports/gaa-04-deployment-readiness-report.md` | Rebuild the gateway before treating runtime `/metrics` as proof of the new baseline output; still complete release env values and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened direct microservice runtime checks: knowledge-service and assistant-service helpers now use `/health/ready`, MCP docgen remains on `/health` because `/health/ready` returns 404, script tests pass 53/53, combined script/runtime-config tests pass 57/57, and current status remains healthy. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Hardened status as a real release gate: `status.sh` still prints the full health table but exits nonzero if any check is unavailable; script tests pass 54/54, combined script/runtime-config tests pass 58/58, and current external-env status remains healthy. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened external-env deployment consistency: `deploy.sh` now uses `--env-file "$ENV_FILE"` for the final Compose `ps` summary instead of falling back to root `.env`; script tests still pass 54/54 and combined script/runtime-config tests pass 58/58. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened first-run usability: `make quickstart` now waits for PostgreSQL, runs selected-env automatic migrations, and then runs runtime validation; script tests pass 55/55 and combined script/runtime-config tests pass 59/59. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened fresh-database determinism: `migrate.sh --auto` now applies `database/schema.sql` when core base schema tables are missing before numbered migrations; script tests pass 56/56 and combined script/runtime-config tests pass 60/60. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Fixed a migration init reliability issue found by code review: `migrate.sh --init` no longer pipes `run_sql_file` directly into `head` under pipefail, and real nonzero `psql` migration failures still print explicit script errors; script tests pass 57/57 and combined script/runtime-config tests pass 61/61. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |
| 2026-06-17 | reviewer/simplifier | GAA-04 | Tightened gateway metrics release gate: `check_gateway_metrics` now requires Prometheus markers plus `gateway_up` and rejects empty or generic metrics output; script tests pass 58/58 and combined script/runtime-config tests pass 62/62. | `reports/gaa-04-deployment-readiness-report.md` | Complete release env values, keep gateway `/metrics` scraped in monitoring, align provider/model rows, seed dynamic route records or document waivers, and rerun full release gates before launch. |

## Known Blockers

- `.env` is absent, so `make validate-config`, `make validate`, full release validation, and provider-backed checks cannot be completed in this session.
- Project-managed E2E startup still requires local dev secrets via `scripts/new/setup-dev.sh --start`, including `POSTGRES_PASSWORD` and `REDIS_PASSWORD`; GAA-02 used the already running local stack with `E2E_REUSE_SERVER=1`.
- Docker compose static config passes with `.env.example`; live service checks require operator-provided secrets and provider keys.
- Frontend lint has 39 warnings and 0 errors; warnings are tracked for later quality work.
- Full assistant-service ruff command has pre-existing lint blockers; touched GAA-01 files pass ruff.
- Broad GAA-03 assistant-test ruff has pre-existing lint blockers in existing files; touched GAA-03 file passes ruff.
- GAA-04 is blocked until root `.env`, chat provider key, embedding provider config, aligned assistant model rows, assistant black-box credentials, and explicit deployment approval are available.
- `make test-isolation` is partial because two assistant black-box checks still skip when assistant isolation or E2E credentials are not supplied; when local E2E credentials are injected, those checks fail because `/assistant/models` is empty.
- Non-destructive live-stack failure isolation now passes the per-route circuit-breaker check against `INTEGRATION_GATEWAY_URL=http://127.0.0.1:8080`; compose-backed stop/start failure-isolation now passes locally with assistant-service, knowledge-service, gateway drain, and breaker checks. The only remaining skip is the `langgraph-agent` case because that service is absent from the local compose stack.
- `make validate` now has a runtime model/provider alignment gate, but the full command still cannot be honestly marked passed until root `.env` exists and config validation succeeds.
- The validate-env model/provider gate now has automated tests in `tests/scripts/test_validate_env_quickstart.py`, including Vertex chat env alignment and docs/compose/env drift coverage.
- `deploy-app` service-scope coverage now asserts app-only updates include gateway, frontend, assistant-service, knowledge-service, and MCP docgen.
- Makefile deployment targets now forward `ARGS` to `scripts/new/deploy.sh`; static tests and `make -n` dry runs cover this.
- `validate-env.sh` provider inference now mirrors `resolve_google("chat")`: Vertex chat backend configures both `google` and `google-vertex`; `VERTEX_CHAT_API_KEY` alone is ignored unless the chat backend is Vertex.
- `validate-config` now uses the same provider inference, so unusable chat key combinations fail in config-only mode instead of waiting for runtime validation.
- `DOCGEN_ARTIFACT_SIGN_KEY` is now required by config validation; docgen's random fallback remains an implementation fallback but is no longer accepted as release-ready configuration.
- The assistant no-model frontend state now has Playwright coverage in `web/e2e/chat-experience.spec.ts`.
- `validate-env.sh --infra-only --config-only` no longer requires app-only secrets and now validates the `postgres redis qdrant` compose subset.
- Infra-only compose validation now uses `--no-interpolate`, so missing app-only compose variables do not block infra-only structure validation.
- Makefile validation, deployment, status, migration, quickstart, and dev-compose targets support `ENV_FILE=/path/to/.env`; deploy and migrate scripts also accept `--env FILE`.
- `status.sh` now forwards the selected `ENV_FILE` to `docker compose ps` before falling back to known container names.
- Makefile stop/restart/logs, backup/restore/list, and dev helper targets also forward `ENV_FILE`.
- `deploy.sh --pull` now uses the selected service set after `--infra` or `--app`, instead of pulling all services first.
- `deploy.sh --infra --app` exits 2 before pre-flight work because partial-deploy modes are mutually exclusive.
- `deploy-app` waits for PostgreSQL before default migrations; `ARGS="--no-migrate"` is the documented restart-only path.
- `migrate.sh --auto` stops deployment on migration SQL errors instead of continuing after an unrecorded failed migration.
- `run_sql_file` now uses `psql -v ON_ERROR_STOP=1` for Docker and local psql execution, so SQL migration files stop at the first error.
- `backup.sh` now accepts `--env FILE`, requires the selected env before creating `backups/` or running backup/restore database operations, and restores with `psql -v ON_ERROR_STOP=1`.
- `backup.sh` and `migrate.sh` now reject unknown options and missing `--env` values before operational actions; `migrate.sh` also rejects missing explicit env files before status/init/auto database access.
- `deploy.sh` and `validate-env.sh` now reject missing `--env` file paths before Docker pre-flight or validation; `deploy.sh` also rejects missing explicit env files before pre-flight.
- `setup-dev.sh` now supports direct `--env FILE`; missing explicit env files fail before status/action, help/status/stop do not require dev DB/Redis passwords, but start/full/reset still do.
- `setup-dev.sh` now uses `QDRANT_HTTP_PORT` before legacy `QDRANT_PORT`, matching the documented env file.
- Phase contracts now include strict-validator-required review evidence, minimal-change scope, and terminal whole-demand regression gates.
- The supplied external env path is readable, but full release validation remains blocked because `REDIS_PASSWORD`, `DOCGEN_ARTIFACT_SIGN_KEY`, `DEFAULT_USER_PASSWORD`, `AUTH_ALLOWED_EMAIL_DOMAIN`, `KNOWLEDGE_CORS_ALLOW_ORIGINS_JSON`, and `ASSISTANT_CORS_ALLOW_ORIGINS_JSON` are missing, placeholders, or invalid for release validation.
- `validate-env.sh` now skips Compose interpolation after earlier required config errors, so the current external env blocker reports actionable config errors instead of an extra Compose interpolation failure.
- Authenticated stable page smoke now covers 11 routes, including current-user `/users/:userId/edit`. The walkthrough also auto-discovers `/knowledge/:datasetId`, `/exams/:examId`, `/share/:shareId`, and `/quiz/:shareCode` when records exist. Current read-only probes for datasets, exams, quizzes, and conversation shares returned HTTP 200 with zero records, so these dynamic route families still need seeded data or an explicit waiver.
- Mocked dynamic-route render smoke now covers `/knowledge/:datasetId`, `/exams/:examId`, `/share/:shareId`, and `/quiz/:shareCode` without writing database records; this improves page render confidence but does not replace live seeded-record release coverage.
- Production compose private-port exposure now has regression coverage: PostgreSQL, Redis, Qdrant, Tempo, knowledge-service, and MCP docgen host ports must stay loopback-bound, and assistant-service must remain expose-only without a host `ports` mapping.
- Full app CORS release validation now requires explicit HTTPS origins and rejects wildcard origins, non-http origins, HTTP origins, and localhost origins for non-local auth domains.
- Full app docgen public URL validation now rejects HTTP, localhost, or loopback `DOCGEN_PUBLIC_URL` for non-local auth domains.
- Frontend container runtime-config generation now has direct pytest coverage for safe defaults and JS string escaping of deploy-time env values.
- Frontend nginx now serves `/runtime-config.js` from an exact no-store location, with static pytest coverage to prevent cache-policy drift.
- Full app frontend browser runtime URL validation now rejects HTTP, localhost, or loopback absolute `VITE_API_URL`/`VITE_API_BASE_URL`/`VITE_TELEMETRY_ENDPOINT` values for non-local auth domains while allowing blank values and same-origin paths such as `/api` and `/telemetry`.
- Full app frontend auth-domain validation now rejects explicit `VITE_AUTH_EMAIL_DOMAIN` mismatches for non-local auth domains while allowing omitted values to inherit from `AUTH_ALLOWED_EMAIL_DOMAIN`.
- Frontend support email now defaults from the active auth domain and full app validation rejects `example.com` support mailboxes for non-local auth domains.

## Clean Exit Checklist

- Phase report written or blocker documented.
- Feature oracle updated only for worked items.
- Validation evidence linked.
- Next target phase and prompt are clear.
