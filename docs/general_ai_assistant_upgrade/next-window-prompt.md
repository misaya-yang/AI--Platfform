# General AI Assistant Upgrade Harness Next Window Prompt

Use this prompt to start a fresh Codex, Claude Code, or Agent Skills-compatible window.

```text
Use $prd-phase-harness to continue the harness at `docs/general_ai_assistant_upgrade`.

Target phase: GAA-04
Target phase file: `docs/general_ai_assistant_upgrade/phase-04-deployment-readiness.md`
Target feature-oracle item: GAA-F005
Current state: GAA-04 is blocked after partial release-readiness validation. Read `docs/general_ai_assistant_upgrade/reports/gaa-04-deployment-readiness-report.md` first; chat-specific Vertex env docs/compose injection are aligned, validate-env config/runtime provider inference mirrors `resolve_google("chat")`, `DOCGEN_ARTIFACT_SIGN_KEY` is required for stable docgen artifact URLs in full app validation, `DOCGEN_PUBLIC_URL` must be HTTPS and browser-reachable instead of HTTP/localhost/loopback for non-local auth domains, infra-only validation is scoped to PostgreSQL/Redis/Qdrant and uses no-interpolate compose structure checks, production compose private-port exposure has static regression coverage for loopback-bound infra/helper ports and expose-only assistant-service, frontend runtime-config entrypoint generation has direct safe-default, support-email defaulting, and JS escaping coverage, `/runtime-config.js` is served from an exact nginx no-store location, frontend browser runtime URL validation rejects non-local HTTP/localhost/loopback absolute `VITE_API_URL`/`VITE_API_BASE_URL`/`VITE_TELEMETRY_ENDPOINT` while allowing blank values and same-origin paths, frontend auth-domain validation rejects explicit `VITE_AUTH_EMAIL_DOMAIN` mismatches for non-local auth domains while allowing omitted values to inherit from `AUTH_ALLOWED_EMAIL_DOMAIN`, frontend support-email validation defaults blank `VITE_SUPPORT_EMAIL` from the active auth domain and rejects explicit `example.com` support mailboxes for non-local auth domains, gateway `/metrics` now has a reachability helper that requires Prometheus markers and `gateway_up`, rejecting empty or generic metrics output, is included in runtime validation plus status output, source-level Prometheus export registers request/error/latency/billing/connection/circuit-breaker metrics plus `gateway_up` for new builds, direct knowledge-service and assistant-service checks use `/health/ready` while docgen stays on `/health` because it has no `/health/ready` route, `status.sh` exits nonzero if any health check is unavailable, deploy summary Compose `ps` uses the selected env file, `make quickstart` waits for PostgreSQL then runs selected-env automatic migrations before runtime validation, `migrate.sh --auto` initializes `database/schema.sql` when core base schema tables are missing before numbered migrations, and `migrate.sh --init` previews captured schema output instead of piping `run_sql_file` directly into `head` under pipefail, full app CORS validation requires explicit HTTPS origin arrays and rejects wildcard origins, non-http origins, HTTP origins, and localhost origins for non-local auth domains, `validate-env.sh` skips Compose interpolation when required config checks already failed, `validate-env.sh --env` rejects missing file paths before validation, `deploy-app` now covers application microservices and waits for PostgreSQL before default migrations, `deploy.sh --env` rejects missing file paths and missing explicit env files before Docker pre-flight, `migrate.sh --auto` stops on SQL errors, `migrate.sh` rejects unknown options, missing `--env` values, and missing explicit env files before status/init/auto migration logic, `run_sql_file` uses `psql -v ON_ERROR_STOP=1`, `backup.sh` accepts `--env FILE`, rejects unknown options and missing `--env` values before action, refuses missing selected env files before creating `backups/` or accessing the database, and restores with `psql -v ON_ERROR_STOP=1`, `setup-dev.sh` accepts direct `--env FILE`, rejects missing explicit env files before status/action, keeps help/status/stop usable before dev DB/Redis passwords are complete, still requires those secrets before start/full/reset, honors `QDRANT_HTTP_PORT`, and forwards `ENV_FILE` to child migrations, `status.sh` passes the selected `ENV_FILE` to `docker compose ps` before falling back to known container names, deploy `--pull` respects partial service scope, partial-deploy modes are mutually exclusive, Makefile deployment targets forward `ARGS`, and formal Makefile operations support `ENV_FILE=/path/to/.env` for validation, deploy, status, migration, stop/restart/logs, backup, and dev helpers. The authenticated site walkthrough now covers visible sidebar routes plus `/knowledge/create`, `/exams`, and current-user `/users/:userId/edit`, passing 1/1 with screenshots for 11 current pages; it also auto-discovers `/knowledge/:datasetId`, `/exams/:examId`, `/share/:shareId`, and `/quiz/:shareCode` when seed records exist. Mocked dynamic-route render smoke now passes 2/2 for `/knowledge/:datasetId`, `/exams/:examId`, `/share/:shareId`, and `/quiz/:shareCode` without database writes, but current read-only live probes returned 200 with zero datasets, exams, quizzes, and conversation shares, so live knowledge/exam detail, public share, and quiz-taking route families still need seeded-record coverage or an explicit waiver. Non-destructive live-stack failure-isolation now passes the per-route circuit-breaker check against public `/api/v1/health`; compose-backed stop/start failure-isolation now passes for assistant-service outage, knowledge-service outage, gateway restart drain, and breaker isolation, with only `langgraph-agent` skipped because it is absent from the local compose stack. Dynamic data probing found read-only `/api/v1/exams` and `/api/v1/assistant/quiz/list` failures when quiz/exam tables or model registry were absent; those list endpoints now return 200 empty lists in the uninitialized local schema state, while quiz generation still requires model registry. The supplied external env at `/Users/.../hejaz_projects/ai_gateway/ai-gateway/.env` is readable through `make validate-config ENV_FILE=...` and `make validate ENV_FILE=...` but still missing, placeholder, or invalid for `REDIS_PASSWORD`, `DOCGEN_ARTIFACT_SIGN_KEY`, `DEFAULT_USER_PASSWORD`, `AUTH_ALLOWED_EMAIL_DOMAIN`, `KNOWLEDGE_CORS_ALLOW_ORIGINS_JSON`, and `ASSISTANT_CORS_ALLOW_ORIGINS_JSON`; formal release gates still need a complete root/external env, provider credentials, aligned enabled model rows, and black-box assistant credentials.

Cold-start protocol:
1. Open `docs/general_ai_assistant_upgrade/README.md`.
2. Open `docs/general_ai_assistant_upgrade/phase-manifest.md`.
3. Open `docs/general_ai_assistant_upgrade/loop-contract.json`.
4. Open `docs/general_ai_assistant_upgrade/loop-state.json`.
5. Open `docs/general_ai_assistant_upgrade/feature-oracle.json`.
6. Open `docs/general_ai_assistant_upgrade/progress-log.md`.
7. Open `docs/general_ai_assistant_upgrade/agent-handoff.md`.
8. Open `docs/general_ai_assistant_upgrade/continuity-ledger.md`.
9. Open only the target phase file and its `PRIMARY_CONTEXT` before planning.

Execution rule:
- Work on exactly one phase and one feature-oracle item.
- Follow the loop cycle: observe, select, execute, verify, record, decide.
- Stay inside the phase edit boundaries.
- Run the required validation and runtime checks.
- Re-run config, runtime health, browser smoke, service isolation, rollback, and monitoring readiness after `.env` and provider credentials are available.
- Do not deploy, run production migration, mutate production data, rotate credentials, or delete Docker volumes without explicit user approval.
- Do not print secret values; summarize only pass/fail and redacted config names.
- Run the GAA-04 validation commands from `phase-04-deployment-readiness.md`; document missing `.env`, provider keys, Docker runtime, or approval as blockers instead of treating them as passes.
- Summarize code facts back into the source packet and continuity ledger before handoff.
- Update the phase report, progress log, handoff file, continuity ledger, and oracle evidence before claiming completion.
- This is the final release gate; record an explicit launch, blocked, or waived decision.
- Stop and document blockers instead of guessing when credentials, production systems, destructive commands, or out-of-scope edits are required.
```
