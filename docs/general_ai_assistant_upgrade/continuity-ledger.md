# General AI Assistant Upgrade Harness Continuity Ledger

**Created:** 2026-06-16

**Harness Folder:** `docs/general_ai_assistant_upgrade`

## Purpose

This file preserves cross-phase continuity between product intent, code facts, execution evidence, and the next agent's starting point.

## Phase Continuity Chain

| Phase | Feature | Depends On | Unlocks | Handoff Boundary | Required Writeback |
| --- | --- | --- | --- | --- | --- |
| GAA-00 | GAA-F001 | none | GAA-01 | baseline report and validation evidence | source-packet code facts, validation results, deployment blockers |
| GAA-01 | GAA-F002 | GAA-00 | GAA-02 | assistant API/runtime report | assistant request/response contracts, service boundary changes |
| GAA-02 | GAA-F003 | GAA-01 | GAA-03 | UI/browser report | route behavior, screenshots, page runtime findings |
| GAA-03 | GAA-F004 | GAA-02 | GAA-04 | AI eval report | golden questions, tool boundaries, privacy/refusal evidence |
| GAA-04 | GAA-F005 | GAA-03 | none | release evidence report | config, health, rollback, monitoring, skipped gates |

## Interface Boundary Ledger

| Boundary | Current Fact | Source | Last Verified | Owner Phase |
| --- | --- | --- | --- | --- |
| Gateway assistant API | `src/api/v1/assistant.py` proxies model/config/dataset/chat flows and lists session artifacts. | local code | 2026-06-16 | GAA-00 |
| Assistant runtime | `apps/assistant-service` owns assistant execution, tools, memory, and agent behavior. | local code | 2026-06-16 | GAA-00 |
| Knowledge runtime | `apps/knowledge-service` owns dataset and retrieval behavior; gateway should use service boundaries. | local code | 2026-06-16 | GAA-00 |
| Shared microservice layer | `ai-gateway-core` owns drain, service proxy, internal auth, idempotency, tracing, storage, and KB proxy client. | local code | 2026-06-16 | GAA-00 |
| Microservice failure-isolation baseline | `tests/integration/test_service_failure_isolation.py` has a non-destructive live-stack mode and compose-backed stop/start coverage. The per-route circuit-breaker isolation check targets public `/api/v1/health` and passes against `http://127.0.0.1:8080`; compose-backed stop/start passes for assistant-service outage, knowledge-service outage, gateway restart drain, and breaker isolation. The `langgraph-agent` stop/start case skips because it is absent from the local compose stack. | `tests/integration/test_service_failure_isolation.py`, live-stack pytest output, compose-backed pytest output, post-test status output | 2026-06-17 | GAA-04 |
| Production compose private-port boundary | Production compose keeps PostgreSQL, Redis, Qdrant, Tempo, knowledge-service, and MCP docgen host ports bound to `127.0.0.1`; assistant-service has no host `ports` mapping and is reachable only through Docker-network `expose: 8093` plus gateway proxy routes. | `docker-compose.yml`, `tests/scripts/test_validate_env_quickstart.py`, script-contract pytest output | 2026-06-17 | GAA-04 |
| Frontend runtime config | `web/src/config/runtime.ts`, `web/public/runtime-config.js`, and `web/docker-entrypoint.d/40-runtime-config.sh` support container-start config injection; the Docker entrypoint keeps the production output path by default, and `web/nginx.conf` serves `/runtime-config.js` from an exact no-store location so deploy-time env changes are not cached like immutable assets. | local code, `web/nginx.conf`, `tests/scripts/test_web_runtime_config_entrypoint.py`, pytest output | 2026-06-17 | GAA-04 |
| Frontend browser runtime URL boundary | Full app validation allows blank or same-origin `VITE_API_URL`/`VITE_API_BASE_URL`/`VITE_TELEMETRY_ENDPOINT` values, but rejects explicit HTTP, localhost, or loopback absolute URLs when `AUTH_ALLOWED_EMAIL_DOMAIN` is non-local. This keeps shared deployments from shipping browser config that calls insecure or user-local API and telemetry endpoints. | `.env.example`, README, DEPLOY, `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| Frontend auth-domain boundary | Full app validation allows omitted `VITE_AUTH_EMAIL_DOMAIN` so Compose can default it from `AUTH_ALLOWED_EMAIL_DOMAIN`, but rejects an explicitly different frontend auth domain when `AUTH_ALLOWED_EMAIL_DOMAIN` is non-local. This keeps username-only login from appending an email domain that backend auth rejects. | `.env.example`, README, DEPLOY, `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| Frontend support-email boundary | Blank `VITE_SUPPORT_EMAIL` now defaults to `admin@AUTH_ALLOWED_EMAIL_DOMAIN` through Compose and to `admin@VITE_AUTH_EMAIL_DOMAIN` in the runtime-config entrypoint. Full app validation rejects explicit `example.com` support mailboxes for non-local auth domains. | `.env.example`, README, DEPLOY, `docker-compose.yml`, `web/docker-entrypoint.d/40-runtime-config.sh`, `scripts/new/validate-env.sh`, tests | 2026-06-17 | GAA-04 |
| Page routes | `web/src/router.tsx` declares public and protected routes; `/assistant` is the target upgrade route. | local code and build | 2026-06-16 | GAA-00 |
| Stable and data-backed page smoke coverage | `web/e2e/site-walkthrough.spec.ts` now walks visible sidebar routes plus stable standalone protected routes `/knowledge/create` and `/exams`, discovers the current user's `/users/:userId/edit` route from `/api/v1/auth/me`, and auto-adds knowledge detail, exam detail, public share, and public quiz routes when seed records exist. The latest current-stack run produced screenshots for 11 pages because read-only seed probes found zero datasets, exams, quizzes, and conversation shares. | `web/e2e/site-walkthrough.spec.ts`, Playwright output, read-only seed probe output | 2026-06-17 | GAA-04 |
| Mocked dynamic route render coverage | `web/e2e/dynamic-route-render.spec.ts` uses Playwright route mocks and client auth seeding to verify `/knowledge/:datasetId`, `/exams/:examId`, `/share/:shareId`, and `/quiz/:shareCode` page rendering without database writes. This supports page-function confidence but does not replace live seeded-record release coverage. | `web/e2e/dynamic-route-render.spec.ts`, Playwright output | 2026-06-17 | GAA-04 |
| Exam and quiz list empty-state contract | Read-only `/api/v1/exams` and `/api/v1/assistant/quiz/list` return empty lists instead of HTTP 500/503 when quiz/exam migrations have not initialized `exams` or `quizzes`; quiz generation still requires model registry. | `src/api/v1/exams.py`, `src/api/v1/quiz.py`, `tests/api/test_quiz_exam_readiness.py`, current-stack API probes | 2026-06-17 | GAA-04 |
| Deployment gate | `.env` is required for `make validate-config`; current local repo has no `.env`. | command output | 2026-06-16 | GAA-00 |
| Artifact schema-missing read contract | Session artifact list returns empty; artifact metadata, download, and delete lookup return public 404 when `assistant.artifacts` is absent. | `src/api/v1/assistant.py`, `tests/api/test_assistant_sessions.py` | 2026-06-16 | GAA-01 |
| Harness tracking | `docs/general_ai_assistant_upgrade/**` is unignored in `.gitignore` so phase artifacts can be committed and handed off. | `.gitignore` | 2026-06-16 | GAA-01 |
| Assistant route E2E contract | `web/e2e/chat-experience.spec.ts` includes executed tests for permitted `/assistant` access and model_tester-only redirect to `/playground`. | `web/e2e/support/helpers.ts`, `web/e2e/chat-experience.spec.ts`, GAA-02 report | 2026-06-16 | GAA-02 |
| Optional Confluence disabled contract | Confluence connection and binding list endpoints return `[]` when the optional sync service is not configured; create/sync operations still require the integration. | `src/api/v1/confluence.py`, `tests/api/test_confluence_disabled.py`, GAA-02 report | 2026-06-16 | GAA-02 |
| API key schema compatibility contract | API key service exposes public `scopes` while falling back to baseline `api_keys.permissions` when the `scopes` column is absent. | `src/services/auth/api_key_service.py`, `tests/services/test_api_key_service_legacy.py`, GAA-02 report | 2026-06-16 | GAA-02 |
| Assistant eval and safety contract | Deterministic tests cover runtime memory prompt-injection neutralization, PII redaction, tool-output caps, bounded KB model summaries, and core privacy/refusal/prompt guardrails. | `tests/services/assistant/test_eval_safety_contracts.py`, GAA-03 report | 2026-06-16 | GAA-03 |
| Deployment readiness gate | Static compose and current local runtime health pass, but formal release validation is blocked until root `.env` and provider credentials are supplied. | GAA-04 report, `Makefile`, `scripts/new/validate-env.sh`, runtime command output | 2026-06-16 | GAA-04 |
| Chat provider env injection | Chat-specific Vertex vars are aligned across docs, example env, compose runtime injection, and validate-env. | `.env.example`, `README.md`, `docker-compose.yml`, `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-16 | GAA-04 |
| Chat provider runtime inference | `validate-env.sh` now mirrors `resolve_google("chat")` for Vertex backend/key combinations before config-only chat-key acceptance and runtime comparison with enabled `llm_models.provider_id`. | `scripts/new/validate-env.sh`, `packages/ai-gateway-core/src/ai_gateway_core/config/endpoints.py`, `apps/assistant-service/src/assistant_service/main.py`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-16 | GAA-04 |
| Docgen artifact signing | Release config now requires `DOCGEN_ARTIFACT_SIGN_KEY`; docgen's per-process random fallback is not considered release-ready because signed artifact URLs would rotate on restart. | `.env.example`, `README.md`, `DEPLOY.md`, `scripts/new/validate-env.sh`, `packages/mcp-docgen-server/src/docgen/storage/signing.py`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-16 | GAA-04 |
| Docgen public artifact URL | Release config now rejects HTTP, localhost, or loopback `DOCGEN_PUBLIC_URL` when `AUTH_ALLOWED_EMAIL_DOMAIN` is non-local, because signed artifact URLs are opened by users' browsers in shared deployments and should be HTTPS. | `.env.example`, `README.md`, `DEPLOY.md`, `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| App-only deployment scope | `deploy-app` now restarts gateway, frontend, knowledge-service, assistant-service, and mcp-docgen-server while leaving infrastructure volumes alone. | `scripts/new/deploy.sh`, `Makefile`, `DEPLOY.md`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-16 | GAA-04 |
| Deployment pull service scope | `deploy.sh --pull` now runs after service selection and passes `SERVICES` to Compose, so infra-only or app-only pull operations do not unexpectedly pull unrelated service images. | `scripts/new/deploy.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-16 | GAA-04 |
| Partial-deploy option safety | `deploy.sh --infra --app` now fails before pre-flight checks because infra-only and app-only are mutually exclusive modes. | `scripts/new/deploy.sh`, `README.md`, `DEPLOY.md`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-16 | GAA-04 |
| App-only migration readiness | `deploy-app` still runs migrations by default, but now waits for PostgreSQL before migration; docs point operators to `ARGS="--no-migrate"` for restart-only app deploys. | `scripts/new/deploy.sh`, `README.md`, `DEPLOY.md`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| Quickstart migration readiness | `make quickstart` waits for PostgreSQL, runs `migrate.sh --auto` with the selected `ENV_FILE`, and only then runs runtime validation. This keeps first local startup from reporting readiness before pending database migrations have run. | `Makefile`, `README.md`, `DEPLOY.md`, `tests/scripts/test_validate_env_quickstart.py`, `make -n quickstart ENV_FILE=/tmp/example.env` | 2026-06-17 | GAA-04 |
| Base schema migration readiness | `migrate.sh --auto` checks the core base tables `services`, `datasets`, `documents`, and `segments`; when any are missing, it applies `database/schema.sql` before pending numbered migrations. This keeps empty first-run databases from relying on gateway startup to create baseline tables before migrations. | `scripts/new/migrate.sh`, `tests/scripts/test_validate_env_quickstart.py`, README, DEPLOY | 2026-06-17 | GAA-04 |
| Migration init preview safety | `migrate.sh --init` captures schema output before showing the first 30 SQL status lines, so `head` cannot close a long `run_sql_file` stream and trip `set -euo pipefail` after a successful schema initialization. | `scripts/new/migrate.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| Automatic migration failure safety | `migrate.sh --auto` now exits nonzero on SQL errors and does not continue after an unrecorded failed migration; nonzero `psql` exits are captured before the script prints explicit migration errors; pending migration detection is safe when no applied row is found. | `scripts/new/migrate.sh`, `README.md`, `DEPLOY.md`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| Migration SQL file execution | `run_sql_file` invokes `psql -v ON_ERROR_STOP=1` for both Docker and local psql paths, so a migration file stops on the first SQL error. | `scripts/new/common.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| Deployment option forwarding | Makefile deployment targets forward `ARGS` to `scripts/new/deploy.sh`, matching the deploy script usage comment. | `Makefile`, `tests/scripts/test_validate_env_quickstart.py`, `make -n` dry-run output | 2026-06-16 | GAA-04 |
| Backup restore safety | `backup.sh` accepts `--env FILE`, rejects unknown options and missing `--env` values before action, requires the selected env before creating `backups/` or running backup/restore database operations, and uses `psql -v ON_ERROR_STOP=1` for restore so rollback SQL errors stop the command. | `scripts/new/backup.sh`, `tests/scripts/test_validate_env_quickstart.py`, negative missing-env and argument-guard command output | 2026-06-17 | GAA-04 |
| Migration argument safety | `migrate.sh` rejects unknown options, missing `--env` values, and missing explicit env files before migration status/init/auto logic, preventing typoed or stale-env operational commands from falling through to the default migration action or database status access. | `scripts/new/migrate.sh`, `tests/scripts/test_validate_env_quickstart.py`, negative argument-guard command output | 2026-06-17 | GAA-04 |
| Infra-only validation boundary | `validate-env.sh --infra-only --config-only` now validates only PostgreSQL, Redis, and Qdrant config plus service-filtered compose config, so infrastructure deployment prep is not blocked by app-only secrets. | `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py`, `docker compose --env-file .env.example config --quiet postgres redis qdrant` | 2026-06-16 | GAA-04 |
| Infra-only compose interpolation boundary | `validate-env.sh --infra-only --config-only` uses `docker compose config --no-interpolate` for `postgres redis qdrant`, so missing app-only compose variables do not block infra-only structure validation. | `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py`, external env validation output | 2026-06-17 | GAA-04 |
| Supplied external env release blocker | `/Users/.../hejaz_projects/ai_gateway/ai-gateway/.env` can be read by `validate-env.sh --env`, but full release config/runtime validation remains blocked by missing, placeholder, or invalid `REDIS_PASSWORD`, `DOCGEN_ARTIFACT_SIGN_KEY`, `DEFAULT_USER_PASSWORD`, `AUTH_ALLOWED_EMAIL_DOMAIN`, `KNOWLEDGE_CORS_ALLOW_ORIGINS_JSON`, and `ASSISTANT_CORS_ALLOW_ORIGINS_JSON`. | `make validate-config ENV_FILE=...`, `scripts/new/validate-env.sh --env ... --runtime` | 2026-06-17 | GAA-04 |
| External env Makefile path | Makefile supports `ENV_FILE=/path/to/.env` for validate, deploy, status, migration, quickstart, and dev-compose targets; deploy/migrate helpers also accept `--env FILE`. | `Makefile`, `scripts/new/common.sh`, `scripts/new/deploy.sh`, `scripts/new/migrate.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| External env operational path | Makefile stop/restart/logs, backup/restore/list, and dev helper targets forward `ENV_FILE`, so operational commands stay on the same external env path as validation and deploy. | `Makefile`, `tests/scripts/test_validate_env_quickstart.py`, `make -n` dry-run output | 2026-06-17 | GAA-04 |
| Status env path and exit contract | `status.sh` passes the selected `ENV_FILE` to `docker compose ps` before falling back to known container names, keeping status metadata on the same env path as validation and deployment. It still prints all checks, but exits nonzero if any health check is unavailable so status can be used as a release gate. | `scripts/new/status.sh`, `tests/scripts/test_validate_env_quickstart.py`, `make status ENV_FILE=...` output | 2026-06-17 | GAA-04 |
| Gateway metrics monitoring boundary | Gateway `/metrics` must be reachable before full runtime validation reports success; output must contain Prometheus markers and `gateway_up`, so empty or generic metrics output fails. `status.sh` reports gateway metrics availability alongside readiness/health checks. | `src/main.py`, `scripts/new/common.sh`, `scripts/new/validate-env.sh`, `scripts/new/status.sh`, `tests/scripts/test_validate_env_quickstart.py`, README, DEPLOY | 2026-06-17 | GAA-04 |
| Gateway Prometheus export boundary | A fresh gateway metrics collector registers request, error, rate-limit, billing, circuit-breaker, active-connection, request-latency, and `gateway_up` metrics for Prometheus export. Histogram bucket, sum, and count lines preserve method/path/service labels. | `src/core/observability/metrics.py`, `tests/core/test_observability.py`, source Prometheus probe output | 2026-06-17 | GAA-04 |
| Direct microservice readiness boundary | Release helpers check knowledge-service `/health/ready` and assistant-service `/health/ready` instead of shallow liveness. MCP docgen keeps `/health` because `/health/ready` is not implemented and returns 404. | `scripts/new/common.sh`, `tests/scripts/test_validate_env_quickstart.py`, direct docker exec readiness probes, README, DEPLOY | 2026-06-17 | GAA-04 |
| Config validation failure order | `validate-env.sh` skips Compose interpolation after earlier required config checks fail, keeping release-gate output on missing/placeholder variables before Compose-level interpolation. | `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py`, external env validation output | 2026-06-17 | GAA-04 |
| Full app CORS release boundary | Full app validation requires `KNOWLEDGE_CORS_ALLOW_ORIGINS_JSON` and `ASSISTANT_CORS_ALLOW_ORIGINS_JSON` to be explicit HTTPS origin arrays for non-local auth domains. Wildcards, missing values, non-http origins, HTTP origins, and localhost origins with non-local `AUTH_ALLOWED_EMAIL_DOMAIN` fail config validation; wildcard, non-HTTP, and HTTP failures now have direct regression tests. | `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py`, README, DEPLOY | 2026-06-17 | GAA-04 |
| Full app frontend auth-domain validation | `VITE_AUTH_EMAIL_DOMAIN` may be omitted to inherit the backend auth domain through Compose, but when explicitly set for a non-local auth domain it must match `AUTH_ALLOWED_EMAIL_DOMAIN`. | `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py`, `.env.example`, README, DEPLOY | 2026-06-17 | GAA-04 |
| Full app support-email validation | `VITE_SUPPORT_EMAIL` may be omitted to use the active auth domain, but when explicitly set for a non-local auth domain it must not keep the local example.com mailbox. | `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py`, `tests/scripts/test_web_runtime_config_entrypoint.py`, README, DEPLOY | 2026-06-17 | GAA-04 |
| Deploy and validation argument safety | `deploy.sh --env` and `validate-env.sh --env` now reject missing file paths before Docker pre-flight or config/runtime validation; `deploy.sh --env FILE` also rejects a missing selected env file before Docker pre-flight. | `scripts/new/deploy.sh`, `scripts/new/validate-env.sh`, `tests/scripts/test_validate_env_quickstart.py`, negative argument-guard command output | 2026-06-17 | GAA-04 |
| Deployment summary env path | `deploy.sh` uses `--env-file "$ENV_FILE"` for the final Compose `ps` summary, matching the env path used for validation, pull, build, up, migration, and runtime validation. | `scripts/new/deploy.sh`, `tests/scripts/test_validate_env_quickstart.py` | 2026-06-17 | GAA-04 |
| Development helper env path | `setup-dev.sh` accepts direct `--env FILE`, rejects missing explicit env files before status/action, allows help/status/stop before dev DB/Redis passwords are complete, still requires those secrets before start/full/reset, honors `QDRANT_HTTP_PORT`, and forwards `ENV_FILE` to child migrations. | `scripts/new/setup-dev.sh`, `README.md`, `DEPLOY.md`, `tests/scripts/test_validate_env_quickstart.py`, external env status and negative missing-env output | 2026-06-17 | GAA-04 |
| Harness quality gates | Every phase contract includes review evidence and minimal-change scope gates; terminal GAA-04 includes whole-demand regression, satisfying the current strict harness validator. | `docs/general_ai_assistant_upgrade/phase-*.md`, harness validator output | 2026-06-17 | GAA-04 |

## Cross-Phase Decisions

- `GAA-01` is complete for the artifact schema-missing read contract and preserved gateway-to-service routing plus tenant isolation.
- `GAA-02` is complete with route E2E and full page walkthrough browser evidence.
- `GAA-03` is complete with deterministic eval/safety evidence and no live provider calls.
- `GAA-04` is blocked by missing root `.env` and provider credentials; deployment remains approval-gated.

## Code Summary Writeback Rules

- `source-packet.md`: update when services, routes, schemas, scripts, tests, or runtime constraints change.
- `continuity-ledger.md`: update when an API contract, state shape, route assumption, eval criterion, or rollback boundary changes.
- `feature-oracle.json`: update only `status`, `evidence`, and `notes` for the active feature.
- Phase report: record command output summaries, skipped checks, blockers, and dependent phase unlock decision.

## Current Continuity Status

- Completed phase: GAA-03.
- Active phase: GAA-04.
- Active feature-oracle item: GAA-F005.
- Current decision: deployment readiness is blocked; partial runtime evidence is healthy, authenticated page smoke now covers 11 pages including current-user edit and can auto-discover dynamic detail/share/quiz routes when records exist, mocked dynamic-route render smoke covers those dynamic page families without database writes, non-destructive live-stack breaker isolation passes, compose-backed stop/start failure-isolation passes locally with only absent `langgraph-agent` skipped, post-test stack status remains healthy, gateway metrics scraping is part of runtime validation and status output, strict metrics content checks now reject empty or generic output and require `gateway_up`, source Prometheus export now registers default metrics plus `gateway_up` and preserves request histogram labels, direct knowledge-service and assistant-service readiness checks use `/health/ready` while MCP docgen remains on `/health`, read-only exam/quiz list empty states are resilient to missing quiz/exam tables, production compose private-port exposure has static regression coverage, full app HTTPS CORS release validation is enforced with wildcard/non-HTTP/HTTP/localhost negative coverage, non-local HTTPS docgen public URL validation is enforced, frontend browser runtime URL validation rejects non-local HTTP/localhost/loopback absolute API and telemetry URLs while preserving blank and same-origin path defaults, frontend auth-domain alignment rejects explicit `VITE_AUTH_EMAIL_DOMAIN` mismatches for non-local auth domains, frontend support-email readiness defaults blank values from the active auth domain and rejects example.com mailboxes for non-local auth domains, frontend runtime-config entrypoint generation has direct safe-default and JS escaping coverage, frontend nginx serves `/runtime-config.js` with no-store cache headers, chat-specific Vertex env injection is aligned, quickstart now waits for PostgreSQL and runs selected-env migrations before runtime validation, `migrate.sh --auto` initializes `database/schema.sql` before numbered migrations when core base schema tables are missing, `migrate.sh --init` avoids pipefail false failures on long schema output, migration and backup-restore SQL errors now stop at the psql/script layers with explicit script errors, backup missing-env checks are side-effect free, backup/migrate/deploy/validate-env/setup-dev argument guards fail before action, deploy/migrate/setup-dev reject missing explicit env files before pre-flight/status/action, deploy final summary uses the selected env file for Compose `ps`, setup-dev status/help stay usable before dev secrets are complete, status uses the selected env path before fallback and exits nonzero on unavailable health checks, strict harness quality gates pass, combined script/runtime-config tests pass 62/62, and formal Makefile operations can use external env files, but formal release validation still requires a complete release env. The supplied external env is readable but incomplete for this repo's release gates; current read-only seed probes found zero datasets, exams, quizzes, and conversation shares, so live knowledge/exam detail, public share, and quiz-taking pages still need seeded-record coverage or an explicit waiver.
- Next action: supply root `.env` and provider credentials, then re-run GAA-04 gates without deployment unless explicitly approved.
