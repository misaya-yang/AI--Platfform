# Agent Studio Continuity Ledger

## Purpose

This is the cross-phase source of truth for interfaces and decisions that later phases consume. Append verified changes; do not overwrite history without recording the superseded decision and report.

## Phase Continuity Chain

| Phase | Oracle | Depends | Produces for next phase |
| --- | --- | --- | --- |
| AS-00 | AS-F001 | none | target-branch capability contract, reachable wiring, test commands |
| AS-01 | AS-F002 | AS-00 | Agent IDs, Draft/Version/ACL/Publication schemas and CRUD contracts |
| AS-02 | AS-F003 | AS-01 | Gateway resolver, signed Runtime Envelope, capability intersection, session/run/trace bindings |
| AS-03 | AS-F004 | AS-02 | MCP/Connector schema, credential principals/grants, secret/auth/health runtime adapters |
| AS-04 | AS-F005 | AS-02 | instruction-only Skill and normalized Knowledge bindings plus revocation/provenance semantics |
| AS-05 | AS-F006 | AS-03, AS-04 | stable Studio/Preview UI and client API contracts |
| AS-06 | AS-F007 | AS-05 | Eval Gate, immutable Version creation, promotion/rollback |
| AS-07 | AS-F008 | AS-06 | Hosted/Embed/API public contracts and channel security |
| AS-08 | AS-F009 | AS-07 | operations, governance, migration/rollback and versioned aggregate manifest |
| AS-09 | AS-F010 | AS-08 | same-build whole-demand evidence and independent terminal release verdict |

## Interface Boundary Ledger

| Boundary | Current decision/fact | Consumer | Owner |
| --- | --- | --- | --- |
| Agent identity | New `agent_id`; do not overload `service_id` | all phases | AS-01 |
| Mutable/immutable split | one optimistic-lock Draft; immutable Version snapshot | runtime, UI, publish | AS-01 |
| Publication | stable channel entry atomically points to Version | hosted/embed/API | AS-01/06 |
| Session | pinned to Agent Version and channel at creation | Assistant, UI, API | AS-02 |
| Capability model | catalog sources remain native/model-native/MCP/Skill/Connector/Knowledge; policy intersection is common layer; AS-00 internal `CapabilityAllowlist` is an immutable tool-name upper bound | ToolInvoker, Studio | AS-00/02 |
| Tool selection | `None` preserves built-in Assistant tools; explicit allowlist, including empty, is applied before relevance selection, after Connector merge, and before invocation/cache return | AgentLoop | AS-00/02 |
| Prompt | platform > Agent Version > channel > capability > memory/RAG > conversation > external data | AgentLoop/Trace | AS-02 |
| Runtime trust | Gateway is the sole Agent resolver and signs an expiring, replay-protected Envelope; Assistant validates but does not re-resolve client Agent fields | Gateway/Assistant/API | AS-02 |
| MCP/Connector | tenant remote Streamable HTTP only in V1; exact schema hash; secret_ref only; service-account/user-delegated grants have owner/scope/audience/revoke/channel constraints | Studio/runtime | AS-03 |
| Skill | Agent Version binds exact `skill_version_id` and full content; user Skills are instruction-only with server-owned `db://` entrypoints | Runtime/Eval | AS-04 |
| Knowledge | normalized fixed Dataset IDs/config, live content, per-run revision fingerprint, run-time ACL; fingerprint is provenance, not deterministic replay | Runtime/Eval | AS-04 |
| Trace | explicit agent/version/publication/channel/fingerprint columns | Eval/Analytics | AS-02/08 |
| Publish | evaluation binds exact Draft revision/spec hash; promotion is atomic/audited | Studio/channels | AS-06 |
| Embed headers | Hosted remains non-embeddable; dedicated Embed document emits allowlist-derived CSP and must pass built Nginx/Helm response-header tests | Web/deployment | AS-07 |
| Existing Assistant | `__builtin_assistant__` remains a compatible fallback under feature flag | release | AS-08/09 |
| Terminal release | AS-08 owns aggregate manifest completeness; AS-09 may not repair features and only orchestrator runs completion gate after actor/critic evidence | release | AS-08/09 |

## Current Code Facts Revalidated by AS-00 on 2026-07-18

- Assistant composition root registers native/Knowledge tools and DB-backed Confluence but no reachable MCPManager initialization was found.
- Gateway advertises MCP routes while its process state sets `mcp_manager = None`; Assistant default MCP policy denies unconfigured MCP tools.
- `AssistantConfig.tools_enabled` is not applied in streaming tool selection. AS-00 added an internal typed allowlist seam; AS-02 owns resolver/Envelope binding into it.
- Tenant tool-policy services are accepted by `AssistantService` but not injected by current `main.py`; when injected, AS-00 makes lookup/filter uncertainty fail closed and authorization precede cached-result return.
- Connector visibility can append definitions after base registry filtering; AS-00 therefore reapplies the allowlist after the merge.
- Skills DB loading reconstructs metadata rather than the immutable version content; API persistence calls appear inconsistent with `save_manifest` signature.
- Skills are bridged after the current turn's tool snapshot is selected, so newly loaded Skill tools are not available to that same turn; exact binding and tenant-safe cache behavior remain AS-04.
- Web uses `__builtin_assistant__` and style-derived system prompts; there is no Agent route/domain.
- Superseded branch fact: current `main`, local `origin/main`, and local `origin/HEAD` are all `945eb2225d644093802bf5f9d75ca4d9dbad6a8d`; the earlier 8-commit drift is no longer present.
- Harness tracking caveat: root `docs/*` still ignores `deploy/runbooks/agent-studio-prd`; AS-00 evidence is local and not claimed Git-tracked.
- Authorized live evidence: the current host lacks the designated Compose-owner checkout, so AS-00 avoided Compose app containers and ran only repository dev PostgreSQL/Redis in Docker plus current-checkout Gateway/Assistant host processes. The final `make test-isolation` result was `6 passed` with no skips; its localhost model stub is contract evidence, not real-provider proof.

## AS-00 Implementation Contract

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-18 | `CapabilityAllowlist(tool_names=frozenset(...))`; absence preserves legacy behavior, explicit empty denies every tool | `test_agent_capability_allowlist.py` | AS-02 must construct it only from the signed effective capability set. |
| 2026-07-18 | Allowlist is enforced in `RegistryToolInvoker`, after Connector merge/before `select_tools`, and again before invocation/cache | `test_assistant_capability_wiring.py` | AS-03/04 capability adapters may only feed catalog/binding inputs; they cannot bypass this upper bound. |
| 2026-07-18 | Tenant policy uncertainty hides/denies and cannot reuse an earlier Knowledge cache hit | actor report plus focused tests | AS-02 may rely on fail-closed runtime enforcement but still must inject the real tenant policy service. |
| 2026-07-18 | Gateway-to-Assistant auth, nonstream and SSE contracts pass through the live local HTTP boundary under the explicit dev-runtime authorization | actor report; `make test-isolation` -> `6 passed` | Existing Assistant compatibility has non-skipped black-box evidence; future phases must preserve the same gate and keep real provider readiness separate. |

## AS-01 Implementation Contract

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-18 | Root migration `071_agent_studio_domain.sql` defines explicit tenant ownership and composite parent/child keys for Agent, member, Draft, binding, Version, Publication, event and token records | live isolated-PostgreSQL migration tests | AS-02 must persist runtime identities using these IDs and must never authorize by UUID without tenant/ACL context. |
| 2026-07-18 | Drafts use `agent-spec/v1`, deterministic SHA-256 spec hashes, monotonically increasing revision and strong ETag/If-Match; stale writes return `AGENT_DRAFT_CONFLICT` with the current revision | repository and HTTP tests | AS-02 Preview must pin an exact saved Draft revision/spec hash; AS-06 Eval/publish must bind the same values. |
| 2026-07-18 | Versions and normalized Version bindings are write-once at the database boundary; Version numbering is serialized by locking the Agent row | migration/repository tests | Runtime and publish code may create/read Versions but cannot patch or delete them. |
| 2026-07-18 | Owner/Editor/Viewer access is server-enforced; cross-tenant/inaccessible UUIDs collapse to 404; the last Owner trigger also retargets `agents.owner_id` when another Owner remains | RBAC and live trigger tests | Studio may render permissions but cannot weaken them; all later repositories inherit tenant plus ACL checks. |
| 2026-07-18 | Agent copy creates a new identity/Draft/sole Owner and drops all runtime/resource bindings until an authorization resolver can prove accessibility | production sanitizer plus API tests | AS-03/04 may add authorized-resource copy after their resolvers exist; they must retain the non-copy list for ACLs, secrets, sessions, memory, Publications and tokens. |
| 2026-07-18 | `/api/v1/agents` is the additive management prefix; mutation responses carry request IDs; public OpenAPI excludes token hashes/secret refs | full Gateway OpenAPI inspection | AS-02/05 extend these resources without changing existing Assistant routes or exposing internal Snapshot/credentials. |

## AS-02 Implementation Contract (verified)

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-18 | `agent-runtime/v1` Snapshot plus `AgentRuntimeEnvelope` is canonical-JSON/hash based and binds issuer, tenant, caller, Agent, Version or Draft revision, Publication/channel, session, request body, spec, issued/expiry time and one-use nonce | AS-02 Actor report; Envelope `27 passed` | AS-03/04 capability adapters provide only server-authorized inputs to the Snapshot; no browser, MCP, Skill, Connector or KB payload may mint or expand trusted Snapshot fields. |
| 2026-07-18 | Gateway owns external Preview/Publication resolution and signs the Envelope only after tenant/ACL/channel/model/readiness checks; Assistant verifies hashes/signature/replay before constructing config or touching a model | AS-02 Actor report; live cross-Agent/cross-tenant/forgery probes | AS-05/07 clients use the closed Preview/Runtime schemas and never submit model, Prompt, capability, Snapshot, Version or Publication overrides. |
| 2026-07-18 | `AgentRuntimeExecutionContext` derives prompt layers, memory principal, idempotency/checkpoint namespaces and trace dimensions from verified state; `CapabilityAllowlist`, exact resolved Skill IDs/normalized tool names and authorized Dataset IDs are immutable upper bounds checked before selection/prompt injection/cache/execution | resolver/isolation/allowlist `23 passed`; two-Skill denial; trace/session/golden `45 passed` | AS-03/04 resolvers may only reduce the signed Version bindings by tenant, caller, channel, credential principal, grant, revocation and readiness; absent/uncertain policy remains empty/deny. AS-04 replaces the current name mapping with exact immutable `skill_version_id` loading without weakening this bound. |
| 2026-07-18 | Migration 072 adds explicit dimensions; forward migration 073 requires positive Preview revision or complete published Agent/Version/Publication identity, proves Version and Publication belong to that Agent/version, and binds checkpoints to the persisted run tenant/user/session scope while legacy rows remain all-null compatible | migration contract `5 passed`; local preflight mismatch counts zero; migration status 69 applied/no pending | Eval/analytics and AS-06/08 query these columns instead of free-form metadata; future schema changes preserve the composite relationships and forward-only rollback evidence. |
| 2026-07-18 | Agent run resume requires current `session_id`, compares both persisted run and checkpoint, rechecks Agent runtime dimensions and refuses cross-session preparation without mutating the rightful run; built-in all-null runs retain the optional legacy probe contract | focused C-01 negative tests; resume/API adjacent regression `45 passed` | Any AS-05/07 resume client must carry its current session; public input can identify the expected session but cannot override persisted ownership/pinning. |
| 2026-07-18 | AgentLoop finish/checkpoint requires this invocation's successful `start_run`; `finish_run` checks session and Agent runtime dimensions before mutating the in-memory mirror and emits the same predicates in SQL | conflicting-session AgentLoop and SQL-predicate negatives; trace/session/golden `45 passed` | Later resume/channel flows must not finalize a run they did not acquire, even when tenant/user/Agent IDs otherwise match. |
| 2026-07-18 | Capability resolver results are authorization keys only; effective Snapshot entries are materialized from the immutable Version binding, so same-ID output cannot lower risk or replace version/schema/config | same-ID metadata substitution negative; Envelope `27 passed` | AS-03/04 health/grant adapters may remove a binding, never rewrite its signed authority metadata; any metadata revision requires a new immutable binding/version. |
| 2026-07-18 | Agent model readiness requires both tenant-enabled metadata and the provider set actually configured in the separate Assistant process; DB-enabled-only and unknown readiness fail closed with `AGENT_RUNTIME_MODEL_UNAVAILABLE` | unit readiness cases plus final stub=false live `503` probe | Open-source runtime changes pass model configuration through the shared service environment; config changes require process recreation/restart until a future authoritative dynamic registry exists. Never silently treat a DB row as executable. |
| 2026-07-18 | Built-in Assistant continues with `agent_runtime=None` and `capability_allowlist=None`; explicit offline stub is opt-in, defaults false, and is evidence-only | AHR groups passed; live isolation `6 passed`; final containers report stub=false | All later phases must retain the generic Assistant API/SSE contract and distinguish offline transport receipts from real provider/model-quality evidence. |

## AS-03 Implementation Contract (verified)

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-18 | Migration 074 separates tenant MCP servers, credential connections, discovered tools, immutable schema snapshots/diffs, exact-schema channel grants and Connector principals; only opaque `secret_ref` values are persisted | AS-03 migration `3 passed`; Actor and Critic reports | AS-05 catalog/config UI may expose redacted readiness and principal state only; it never receives credential values. |
| 2026-07-18 | Tenant MCP is Streamable HTTP only. Create/update reject non-global literals and DNS sets containing any unsafe address before persistence; runtime repeats validation, connects to the validated IP literal and preserves original Host/TLS SNI | API `6 passed`; security `19 passed`; amended iteration-3 Critic approval | Later UI may show stable denial codes but cannot offer tenant stdio, private-network exceptions or TLS bypasses. Platform-managed private services remain operator configuration, not tenant input. |
| 2026-07-18 | Remote catalog risk/read-only hints are untrusted and normalized to medium/not-read-only; schema compatibility is recursive and conservative; Version runtime/publish requires the exact immutable tool/version/schema hash | PostgreSQL/API/runtime tests plus preserved C-02/C-03/C-03b findings | AS-05 displays source/risk/schema-diff facts from the authoritative registry; AS-06 must block stale/incompatible bindings rather than silently refreshing them. |
| 2026-07-18 | Public/embed MCP requires a Tenant Admin's read-only service-account grant bound to the exact current schema hash and channel; delegated execution resolves only the current user's unexpired, unrevoked scope/audience grant | principal, public-grant and schema-drift tests; Critic approval | AS-07 cannot reuse user-delegated or Admin credentials for anonymous callers and cannot expose write-capable MCP by remote annotation. |
| 2026-07-18 | MCP and existing Confluence Connector are injected only beneath the AS-02 immutable capability allowlist and reauthorized immediately before invocation; `AGENT_STUDIO_MCP_ENABLED=false` removes Agent external capabilities without changing the built-in Assistant path or deleting registry history | Assistant runtime/policy/Connector `23 passed`; isolation `6/6`; AHR `28/77/8/98` plus golden | AS-04 capability work may only reduce this upper bound. AS-08 rollback retains additive rows and verifies the legacy Assistant independently. |
| 2026-07-18 | Real third-party OAuth/Secret Store success is deferred until an approved credential/writer exists; mock protocol evidence is not production-provider proof | Actor/Critic waiver judgment; `provider_calls=0` | Deployment readiness must supply production Secret Store, issuer/egress policy and an authorized smoke without reopening the Agent authorization model. |

## AS-04 Implementation Contract (verified)

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-18 | Migrations 075 and 076 normalize tenant/user Skill artifacts, immutable exact versions, Draft/Version Skill and Dataset bindings, reverse references and an authoritative retrieval-content revision that excludes operational telemetry | migration `4 passed`; Actor/Critic reports | AS-05 edits binding resources through server APIs and shows exact version/revision state; AS-06 seals the same normalized rows and never rewrites an old Version. |
| 2026-07-18 | Tenant Skill uploads are instruction-only and receive server-owned `db://<skill_id>/<version_id>` identities; full canonical SKILL.md content and hash are tenant/user/version-authorized, immutable and loaded through a request-scoped registry/tool overlay before first-turn tool selection | required Skill gates `26/6`; Critic C01-C05 closure | Studio must not expose source/path/network/executable entrypoints or imply that user Skills execute code; later runtime/eval consumes only the exact selected version beneath the AS-02 capability upper bound. |
| 2026-07-18 | Knowledge bindings are normalized by stable Dataset ID with exact per-Dataset `mode`, `top_k`, `threshold` and `include_images`; save, publish and every run recheck current caller ACL, deletion and revocation | Knowledge/streaming `45`, resolver `8`, running-container PostgreSQL policy probe | AS-05 may display effective binding/config and current unavailable state but cannot calculate authorization client-side; AS-06/07 re-resolve current authority before publish/run. |
| 2026-07-18 | Production Assistant wiring injects a DB-backed Agent resource policy and tenant tool policy. It can only intersect the signed Snapshot. AgentLoop pre-retrieves every `auto` Dataset, exposes KB tooling only for `tool`, omits `off`, treats retrieved context as untrusted lower-priority data, and stops before the model when all automatic retrieval fails | Critic iteration 4 approval; mixed/failure provider-free nodes; production resolver and live policy probe | Preview UI can distinguish automatic retrieval, model-selected Knowledge and disabled bindings, but must not present a model answer after stable Knowledge-unavailable termination. |
| 2026-07-18 | Dataset fingerprints include retrieval-effective non-secret configuration and authoritative content revision while excluding credentials, telemetry and mutable derived counters; every run records `content_mode=live_latest` and `historical_replayable=false` | fingerprint `5`, migration `4`, durable AS-04 golden | Eval/Version diff may use fingerprints for provenance and drift localization only; no UI or release artifact may promise historical content replay without retained evidence. |
| 2026-07-18 | `AGENT_STUDIO_SKILLS_ENABLED=false` and current resource revocation fail closed for Agent paths without deleting exact bindings; generic Assistant paths retain `agent_runtime=None` compatibility | AHR `33/77/8/98` plus golden, live isolation `6/6`, canonical Critic approval | AS-05/08 must preserve feature-flag rollback and the built-in Assistant route independently of Studio availability. |

## AS-05 Implementation Contract (verified)

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-19 | Studio persists name, description, spec, normalized resources and the next Draft revision through one `If-Match` transaction; 409/422/503 and post-metadata PostgreSQL failure leave the entire prior batch unchanged | Agent API `9`, real PostgreSQL rollback, deterministic/live save receipts, canonical Critic approval | AS-06 Eval and publish must bind only an exact saved Draft revision/spec/resource fingerprint; unsaved or partially represented browser state cannot become release evidence. |
| 2026-07-19 | Draft and historical Version Preview use closed server-resolved target identities, isolated session namespaces and server-projected public SSE fields; target/revision changes cannot hot-swap an existing session | Agent E2E `25`, resolver/raw-SSE `11`, Preview golden, Actor/Critic reports | AS-06 promotion/rollback must preserve existing session pinning and may reuse Preview components only with immutable Version and Publication facts supplied by the server. |
| 2026-07-19 | Owner/Editor/Viewer UI mirrors server-returned role but never replaces repository authorization. Editor save/Preview executes; Viewer remains read-only; mobile actions, keyboard/focus and exact 1440/1024/390 evidence are durable | full OSS E2E `31`, 32 labelled screenshots, axe zero serious/critical, canonical Critic approval | AS-06 Eval/Publish/rollback UI must keep server-authoritative denial, full role matrix, exact desktop/mobile evidence and no hidden-button authorization. |
| 2026-07-19 | `VITE_AGENT_STUDIO_ENABLED=false` removes Agent routes/navigation through base Compose runtime config while preserving `/assistant`; true restore and provider-unavailable live Preview are failure-closed | runtime-config/Compose `5`, built-container false/true browser receipts, live screenshots | AS-06 mutations must remain separately disableable/read-safe and cannot make existing Assistant availability depend on Agent Studio or provider success. |

## AS-06 Implementation Contract (verified)

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-19 | A release evaluation is an immutable server-computed identity over tenant/Agent/Draft ID/revision/spec, model/provider authorization, prompt/tool/Skill/Knowledge/runtime/Snapshot, channel policy and an authorized Eval Dataset ID/version/canonical manifest hash over metadata plus sorted full examples. The browser cannot submit a trusted spec, fingerprint, profile, pass, threshold or waiver | candidate `13`, API/PostgreSQL/gate `31`, Actor report and Eval matrix | AS-07 may consume only a Publication/Version produced by this gate; it cannot add a client-side publish bypass, Knowledge Dataset substitution or per-channel Snapshot override. |
| 2026-07-19 | The open-source `offline_v1` profile evaluates release integrity/resource/secret safety, records zero provider cost and explicitly sets `model_quality_evaluated=false`; any unconfigured production profile fails closed | gate/API production-profile negatives, golden/Eval regression, live model-unavailable receipt | AS-08 owns deployment Dataset/threshold/waiver inputs. Hosted/Embed/API copy must not describe `offline_v1` as production model quality. |
| 2026-07-19 | Evaluations persist `queued`, claim `running` through a separate authorized execution boundary, and finish as immutable `passed`, `failed` or `cancelled`; PostgreSQL rejects invalid transitions and makes cancel-versus-complete terminal races deterministic | API/PostgreSQL lifecycle tests, release browser `10`, live provider-free lifecycle receipt | AS-07 may display and poll durable server truth but cannot manufacture terminal state, pass status or cancellation locally. |
| 2026-07-19 | Promotion binds one Eval to one sealed immutable Version and atomically updates one Publication pointer, append-only event and completed idempotency request. A tenant+operation+key lock precedes Agent work; durable replay occurs before mutable resolution and is checked again under lock | cross-Agent two-connection race, API/PostgreSQL/gate `31`, atomicity JSON | AS-07 receives stable `publication_id`, `public_id`, channel/auth/policy and `version_id`; retrying delivery setup must not create or mutate Versions or leak raw database conflicts. |
| 2026-07-19 | Promotion and rollback lock/recheck the exact tenant model/provider enabled state, access level and runtime-readiness proof together with current MCP/Connector/Skill/Dataset authority. Rollback targets only a non-current Version in that Publication's history and moves pointer/event/request in one transaction | injected model/resource races, revoked/non-historical/failure tests and browser rollback cases | AS-07 session creation resolves the current pointer only for new sessions. Existing sessions retain their AS-02 pinned Version; authorization drift leaves all release rows and pointers unchanged. |
| 2026-07-19 | Eval list/detail recompute the current trusted candidate and return explicit Draft/model/resource/Dataset/runtime/release/evaluation identity stale reasons without rewriting immutable evidence | API/browser stale cases and real Dataset content-drift boundary | AS-07 operations surfaces must show server stale reasons and cannot infer currency from Draft revision alone. |
| 2026-07-19 | Structured release diff is prompt-free and release audit reasons matching shared Secret redaction patterns are rejected before persistence | candidate diff and API schema tests; desktop/mobile/dark rendered receipts | Public channel configuration and operations UI may expose hashes/lengths/change categories, never Prompt text, Secret refs, credentials or signed Snapshot data. |
| 2026-07-19 | Iteration-2 Critic proved persisted model/provider row locks are insufficient for current runtime readiness: the database branch returns before its supplied revalidator, so revoked provider readiness can still commit promotion or rollback | canonical/iteration-2 Critic verdict; direct callback count 0; disposable PostgreSQL wrong writes | C-06 must be closed before this provisional release contract is verified. AS-07 may never treat catalog-enabled as equivalent to currently executable provider readiness. |
| 2026-07-19 | Actor iteration 3 requires the current provider-readiness revalidator while model/provider rows remain locked, compares the complete proof and fails closed on missing/changed/unavailable evidence before mutation | focused PostgreSQL 2/2; Publish 0/0/0/0, Rollback pointer/counts unchanged; required group 33 | Fresh Critic must reproduce this closure. Downstream delivery may rely on the Publication only after current readiness and locked catalog authorization agree in the same decision boundary. |
| 2026-07-19 | Iteration-3 Critic closed C-06 but proved Eval example content could change after manifest resolution while Publish still committed; Actor iteration 4 locks the Dataset parent `FOR UPDATE` and every manifest example `FOR SHARE` | preserved iteration-3 verdict; focused PostgreSQL update/insert 2/2; required group 35 | Fresh Critic must confirm update/delete and insert/import cannot cross the manifest decision. AS-07 may consume only a Publication whose Eval content stayed stable through commit. |
| 2026-07-19 | Fresh iteration-4 Critic independently confirmed Dataset-parent/example lock ordering and both update/insert serialization paths, then approved AS-06 | hashes 13/13, Ruff, real PostgreSQL 2/2, exact release group 35/35 and supported claim check | AS-07 may resolve immutable Publications; it cannot bypass publication readiness or repin an existing session. |

## AS-07 Implementation Contract (verified)

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-19 | Hosted, Embed and Runtime API accept only stable Publication identities and closed client request fields; Gateway resolves the current immutable Version and signs the existing AS-02 Runtime Envelope | Actor report; Runtime/security 24; browser 8; Critic iteration 2 | AS-08 trace/metric/audit filters use explicit Agent/Version/Publication/channel identities and must not reconstruct them from browser metadata. |
| 2026-07-19 | Runtime tokens are stored as SHA-256 hashes and raw values are shown once; scope, expiry, rotation, revocation and last-use are rechecked for the exact Publication | API/security and real PostgreSQL tests; channel-security JSON | AS-08 revocation/deletion must include Runtime tokens without exposing hashes/raw values or deleting immutable Version/publication evidence. |
| 2026-07-19 | Shared Redis Lua enforcement atomically applies principal, trusted-IP and Publication minute/day buckets; Embed renewal preserves a deterministic HMAC abuse identity and limiter absence fails closed | multi-limiter/concurrent negatives; live two-client Redis probe; Critic C-01 closure | AS-08 owns quota observability/admin thresholds and must preserve all three dimensions plus fail-safe enforcement. |
| 2026-07-19 | Idempotency is durable `pending/completed/failed`; one caller executes and a completed exact retry replays the persisted status/media/session/SSE bytes, while pending/failed duplicates cannot charge quota or invoke tools/model | retry/concurrency API tests; two-client PostgreSQL reservation/replay; Critic C-02 closure | AS-08 retention/deletion must handle terminal response bodies and attachment metadata without reopening duplicate execution. |
| 2026-07-19 | Hosted/Embed/API attachment upload returns an opaque handle scoped to tenant, Publication, principal, channel and expiry. Only Gateway resolves storage paths; Assistant accepts the signed closed shape and confines it to `/uploads/` using shared storage | API/Assistant/browser tests; shared Gateway/Assistant storage probe; Critic C-03 closure | AS-08 deletion/retention/cache cleanup must remove attachment objects and metadata consistently while preserving redacted audits. |
| 2026-07-19 | Ordinary Hosted/SPA responses remain anti-framed; only dedicated Embed HTML uses exact allowlist-derived CSP with no XFO, short-lived exact-Origin credentials and source/origin/version-checked postMessage | deployment 5 plus config/built-image smoke; browser 8 | AS-08 feature-flag rollback must hide public entry points without weakening existing Assistant routes or global framing headers. |
| 2026-07-19 | Fresh iteration-2 Critic matched 24/24 fingerprints, independently passed 40 tests with zero failures/skips plus the config-only gate and approved C-01 through C-03; the supported claim check exited 0/100 but is metadata-only | canonical and preserved Critic verdicts; Feature Oracle AS-F008 | AS-08 is unlocked. AS-09 same-build whole-demand regression and terminal completion remain unclaimed. |
| 2026-07-20 | Operations summaries derive TTFT, tool success, Knowledge hit and feedback-positive rates from explicitly Agent/Version/Publication/channel-filtered trace rows; recursive at-rest redaction and explicit binding/high-risk audit events preserve the same dimensions | AS-08 operations tests `24/24`; live authenticated metrics/audit probe; Actor report | AS-09 may consume only these explicit server dimensions and may not infer them from UI metadata. |
| 2026-07-20 | Agent count, publication count, concurrency, daily-token, MCP and storage quotas are authoritative cross-worker limits with fail-closed lookup and threshold audit; deletion is retryable, legal-hold-safe and complete across tokens, sessions, memory, attachments, caches and derived content | migration 081; governance/deletion tests; live PostgreSQL terminal replay and denial | Terminal deletion receipts replay only to the requester or Tenant Admin after Agent/ACL cleanup; active-Agent last-owner protection remains intact. |
| 2026-07-20 | The versioned aggregate contains 39 required AS-00 through AS-08 gates and rejects numeric skipped-test summaries even when a child exits zero; the final AS-08 Critic matched 30/30 fingerprints and approved after exact operations `24/24` plus focused PostgreSQL replay | aggregate contract `5/5`; AS-08 manifest; canonical iteration-3 Critic verdict | AS-09 must execute this manifest against one stable source snapshot without feature repairs, then obtain a separate terminal Critic before completion claim check. |

## AS-09 Terminal Contract (verified)

| Date | Contract | Evidence | Downstream impact |
| --- | --- | --- | --- |
| 2026-07-20 | Terminal completion is based only on candidate 3: the unchanged AS-08 manifest executed 39/39 required gates with zero skips/failures on stable source `2ffa4684a3055b123b51b779eef9321e0821c1940c2942c10b34a2c054f14115`; all result/manifest/log hashes independently matched | AS-09 aggregate result, build receipt, whole-demand matrix and approved terminal Critic | Any future source/test/manifest change invalidates this same-build receipt and requires a fresh complete aggregate; partial candidates or targeted retries cannot be combined. |
| 2026-07-20 | Whole-demand evidence includes actual built Nginx/Gateway Hosted/Embed headers, exact desktop/tablet/mobile browser and accessibility matrices, additive migration/rollback, tenant/security/privacy and built-in Assistant AHR/isolation | 39 accepted logs; AS-09 Actor/Critic reports | No downstream release may replace built-header evidence with Vite, mock UI evidence with production claims, or local provider-free runtime evidence with model-quality claims. |
| 2026-07-20 | Final local decision is `ready-but-not-deployed`; external provider quality, production Secret/OAuth/egress controls, monitoring credentials/window and explicit rollout authorization remain deployment prerequisites | AS-09 release decision and fresh terminal Critic approval | A future deployment requires its own authorized smoke, owner, monitoring and rollback thresholds; this completion does not authorize deployment or production mutation. |

## Writeback Rules

- Add a dated row when an API, schema, prompt layer, capability, channel, auth, failure or migration contract changes.
- Link the actor report and critic verdict for every change.
- Update downstream Phase contracts before unlocking them if likely edit paths or validation commands change.
- Security revocation behavior and existing Assistant compatibility cannot be waived implicitly.

## Current Status

- Verified: AS-00 / AS-F001 (`passing`).
- Verified: AS-01 / AS-F002 (`passing`) after separate Actor/Critic evidence and an exit-zero strict Phase completion gate with quality score 100.
- Verified: AS-02 / AS-F003 (`passing`) after the iteration-3 fresh Critic approved all resolver/isolation corrections and the Phase completion gate passed.
- Verified: AS-03 / AS-F004 (`passing`) after three preserved finding rounds, an amended iteration-3 independent approval, every required no-skip gate, and an exit-zero supported claim check. The installed validator's legacy `--strict` mode is unsupported; no required implementation/security evidence was waived.
- Verified: AS-04 / AS-F005 (`passing`) after three preserved `changes_requested` findings, a fresh iteration-4 approval, every required no-skip gate, real local PostgreSQL policy/revocation evidence and an exit-zero supported claim check. The obsolete `--strict` path was skipped by explicit user direction; no current implementation, security, compatibility or Critic gate was waived.
- Verified: AS-05 / AS-F006 (`passing`) after two preserved `changes_requested` reviews, fresh iteration-3 approval, exact zero-skip Actor/Critic gates, executed Editor and mobile/viewport receipts, and an exit-zero supported claim check with structure score 100/100. Its first claim-check invocation correctly rejected running-loop metadata and was not counted as a pass.
- Verified: AS-06 / AS-F007 (`passing`) after three preserved `changes_requested` rounds, iteration-4 C-07 closure, final zero-skip Actor gates, independent hashes/Ruff/PostgreSQL/release-group approval and an exit-zero supported claim check with structure score 100/100. The validator's metadata-only limitation is recorded.
- Verified: AS-07 / AS-F008 (`passing`) after one preserved `changes_requested` review, iteration-2 closure, zero-skip Actor gates, an independent 40-test/24-fingerprint approval and an exit-zero supported claim check. The validator's legacy-compatible check proves metadata consistency only; strict certification is unavailable for the current v2 Harness.
- Verified: AS-08 / AS-F009 (`passing`) after three preserved Critic rounds, final 30/30 fingerprint approval, exact zero-skip operations/aggregate/browser/isolation/Assistant compatibility evidence and an exit-zero supported claim check.
- Verified: AS-09 / AS-F010 (`passing`) after the accepted 39/39 zero-skip stable-source aggregate and a fresh independent terminal Critic approval with no blockers.
- Next action: complete the blocking-issue review and authorized logical commit/push workflow. The supported post-Critic completion claim check exited 0 with diagnostic structure 100/100; deployment remains a separate future authorization.
