{
  "program": "kb-rag-ui-t5",
  "title": "KB 升级 PRD · T5 前端切片（与后端 session 并行）",
  "created": "2026-08-28",
  "owner_session": "kb-frontend worktree",
  "branch": "worktree-kb-frontend",
  "base_ref": "worktree-kb-rag-upgrade@7a7fc3a",
  "plan_file": "~/.claude/plans/iridescent-tickling-kahn.md",
  "prd": "（后端 worktree 未提交）docs/plans/rag-upgrade-prd-2026-08.md + addendum",
  "boundary": {
    "owned": [
      "web/src/pages/knowledge/**",
      "web/src/api/knowledge.ts",
      "web/src/api/eval.ts (KB 用法)",
      "web/src/types/knowledge.ts",
      "web/src/hooks/useKnowledge.ts",
      "web/src/i18n/locales/*.json knowledge.* 段",
      "web/e2e/knowledge-*.spec.ts"
    ],
    "forbidden": ["apps/", "src/（网关）", "database/", "Makefile", "harness.yml", "uv.lock", "PRD 文档", "另一 worktree 的任何写入"],
    "docker_hot_update_migrations_live_ui": "串行，归后端 session 窗口"
  },
  "backend_dependencies": {
    "D1": "list_documents SELECT 补 enabled/archived/阶段时间戳（阻塞 §5-#9/#4 全绿）",
    "D2": "archived_reason 列 VARCHAR(255) vs schema 2000",
    "D3": "反馈表+端点（§5-#25 前提）",
    "D4": "文档/段落列表分页参数",
    "D5": "迁移 100 遥测读端点（§5-#26 前提）",
    "D6": "ChunkingConfigSchema（extra=forbid）字段集 < chunking.py 运行时字段集（extract_metadata、metadata_fields、normalize_whitespace、strip_html、page_marker、segmentation 等）——PUT /config 无法往返运行时字段，元数据抽取设置无法持久化；前端已按 allow-list 降级（C3），待 schema 扩宽"
  },
  "notes": {
    "unit_tests": "node --test 'web/src/**/*.test.ts'（package.json 无 test script，Node>=22.18 原生 strip-types）",
    "type_check_gate_broken": [
      "pnpm -C web type-check（tsc --noEmit 打 solution 式 tsconfig，files:[]）实际检查 0 个文件——main 上即已损坏，此前所有 type-check 绿均为空操作",
      "真实命令：cd web && pnpm exec tsc --noEmit -p tsconfig.app.json --ignoreDeprecations 6.0（TS 6.0.3；baseUrl 弃用告警需此旗标）",
      "真实 tsc 下全库存量 98 个类型错误（assistant/eval/dashboard/chat 等，非本计划范围）",
      "本计划纪律：KB 切片文件以真实 tsc 为准，不新增错误；修脚本文案属共享基建，越界，交由用户裁决",
      "src/**/*.test.ts 报 node:* 模块缺型（全库惯例，node --test 运行不经 tsc），不计"
    ],
    "c1_findings": [
      "上传对话框 rerank 默认 enabled=true，与平台默认 false 不一致——行为保持不动，C3 决策",
      "e2e mockPreset 是服务端检索预设夹具（fast/balanced），非 UI 默认值复制，单一来源改造不涉及",
      "hit-test scoreThreshold 初始 0（0=不过滤）与 fusionMethod 初始 weighted 为工作台语义，刻意保留",
      "默认值按多数值收敛：wizard chunk 600→500、wizard threshold 0.2→0.3、hit-test denseWeight 0.5→0.7、types 死常量 alpha 0.75→0.7"
    ],
    "c2_debt": [
      "DocumentsTab:462 variant=\"outline-solid\" 不在 Button 变体并集内（运行期 cva 忽略，渲染同 monolith）——需视觉判断+Button 属共享组件，留 C8 设计轮",
      "DatasetUploadDialog 卡片 transition-all（既有；motion 静态扫描挂账，随设计轮收敛）"
    ]
  },
  "active_phase": "C6 文档归档+启用禁用开关+409 队列语义+mock e2e",
  "iterations": [
    {
      "phase": "C1",
      "date": "2026-08-28",
      "scope": "types+默认值单一来源+deriveDisplayStatus",
      "result": "done",
      "evidence": "type-check/lint/build/i18n:check 绿；node --test 46/46（含 12 新测试）",
      "changes": [
        "types/knowledge.ts: DocumentStatus 补齐在网词表；+DocumentDisplayStatus/阶段时间戳/归档字段；+deriveDisplayStatus+resolveDisplayStatus（镜像后端 document_service 合同，未知态 fail-closed→indexing）",
        "DEFAULT_CHUNKING/RETRIEVAL_CONFIG satisfies 化+基线值冻结（单测锁定）",
        "默认值单一来源接入：DatasetDetail（hit-test/设置编辑/配置载入回退/展示回退）、DatasetCreate 向导、useDatasetUploadController",
        "e2e mockPreset 不动（服务端预设夹具）"
      ]
    },
    {
      "phase": "C2",
      "date": "2026-08-29",
      "scope": "拆分 DatasetDetail.tsx（3934 行→349 行壳 + 7 个 detail/ 模块，纯搬运）",
      "result": "done",
      "evidence": "e2e:opensource 48/48（先修 C1 回归至绿）；node --test 46/46；lint/build(oversizedRoutes=[])/i18n:check 绿；真实 tsc 下 KB 切片零新增错误（存量仅剩 outline-solid 一项挂账）",
      "changes": [
        "壳只留：数据集加载、?tab= 路由、页头/错误带、对话框开关态、共享 hit-test 束（≤350 行预算达标）",
        "documents/retrieval/qa/settings/permissions 常驻挂载+hidden 切换（保留状态），eval/sources 维持条件挂载",
        "修 C1 回归（因 type-check 门损坏漏网，e2e 捕获）：useDatasetUploadController 缺 DEFAULT_RETRIEVAL_CONFIG 导入（错误边界 ReferenceError）；satisfies 常量字面量泄漏进 useState（\"automatic\"/false 态破坏 setter/switch）——常量泄漏字段显式加宽 + 调用点窄化回 ChunkingMode",
        "既有类型债（真实 tsc 首查暴露，均 type-level 零行为变化）：FILE_TYPE_CATEGORIES as-const、QA_MODEL_OPTIONS provider、RetrievalEvalWorkbench datasetId ?? \"\""
      ]
    },
    {
      "phase": "C3",
      "date": "2026-08-29",
      "scope": "配置有损保存修复（分块 REPLACE→allow-list 全量往返 / 检索深补丁 / 向导补 fusion / 上传对话框合同对齐）",
      "result": "done",
      "evidence": "e2e:opensource 48/48；node --test 48/48（+2 新）；lint/build(oversizedRoutes=[])/i18n:check 绿；真实 tsc 98=存量基线，KB 切片零新增",
      "changes": [
        "api/knowledge.ts: updateDatasetConfig 按 PUT 语义定型——chunking_config=Record（schema extra=forbid 且字段超出本地类型），retrieval_config=RetrievalConfigPatch 深 partial；注释固化 replace vs patch 语义",
        "types/knowledge.ts: +CHUNKING_CONFIG_API_FIELDS（后端 ChunkingConfigSchema allow-list 镜像，排除 regex_pattern）+SAFE_CHUNK_HEADING_PATTERNS+isSafeHeadingPatterns；单测锁定（负向探针含 regex_pattern/extract_metadata/metadata_fields）",
        "SettingsTab 分块保存：预校验（regex 拦截/overlap<size 拦截）+ 存储配置经 allow-list 过滤往返再叠加编辑值（修 REPLACE 语义把运行时字段打回 schema 默认；regex_pattern 与非安全三元 heading_patterns 永不往返）",
        "SettingsTab 检索保存：去掉硬编码 rrf_k:60（深合并保留存储值）+ 删恒等 modeMap",
        "DatasetCreate 向导：index_config.retrieval 补 fusion（创建即整体存储，缺省会落 schema 默认）",
        "上传对话框：开始上传前预校验（正则禁用/重叠规则镜像 ChunkingConfigSchema，不发字段的模式按 schema 默认 2000/300 计，hierarchical 子块规则）；元数据抽取改为只警告不可持久化（D6）不再进 config 载荷（此前会 422 整个配置更新）；嵌入选择同步数据集现值、仅变化时下发（修每次上传静默换模型）",
        "决策：上传对话框 rerank 默认 ON 保持（用户可见控件，冻结基线纪律；patch 语义下不污染其他字段）"
      ]
    },
    {
      "phase": "C4",
      "date": "2026-08-29",
      "scope": "轮询条件化 + 300ms 防抖 + 409+Retry-After 列表读静默重试",
      "result": "done",
      "evidence": "e2e:opensource 48/48；node --test 48/48；lint/build(oversizedRoutes=[])/i18n:check 绿；真实 tsc 98=存量基线",
      "changes": [
        "useDocuments refetchInterval 条件化：仅当存在 display_status ∈ {queuing,indexing} 的行时 2s 轮询（resolveDisplayStatus 双保险，未知态 fail-closed 继续轮询直至后端澄清）；空闲数据集零请求",
        "useSegments：移除常驻 5s 轮询，改按需（打开面板按 staleTime 过期重取 + 变更后 invalidate）",
        "+useDebouncedValue（hooks/useKnowledge 导出，默认 300ms）：文档搜索（客户端过滤）与段落搜索（直连服务端）接入；防抖值同时是 query key，手动 invalidate 改用同一变量防 key 漂移",
        "withConflictRetry：列表读遇 409 带 Retry-After 等待（封顶 5s、默认 1s）重试一次，不弹错误带（并发生成切换）"
      ]
    },
    {
      "phase": "C5",
      "date": "2026-08-29",
      "scope": "段落操作面：单条开关（PATCH /status）+批量启停（/segments/batch/enable ≤500 部分成功）+全字段编辑（text/answer/keywords 一次往返）",
      "result": "done",
      "evidence": "新增 e2e/knowledge-segment-ops.spec.ts 4/4 + 既有 knowledge @mock 4/4（--grep @mock 共 8/8）；node --test 50/50（+2 parseSegmentKeywords）；lint/build(oversizedRoutes=[])/i18n:check 绿；真实 tsc 98=存量基线，KB 切片零新增",
      "changes": [
        "api/knowledge.ts: updateSegment 改发 SegmentUpdateFields{text,answer?,keywords?}（PUT，extra=allow 下三字段一次往返，修 §5-#13 只发 text 丢 answer/keywords）；+setSegmentEnabled（PATCH /segments/{id}/status 为正式端点，POST /enable 仅兼容）；+batchSetSegmentsEnabled（POST /segments/batch/enable，segment_ids 1..500、enabled 双向、{success,updated,total} 部分成功）",
        "types/knowledge.ts: +parseSegmentKeywords（ASCII/全角逗号、顿号切分，trim、去空、保序去重）",
        "ChunkCard/SegmentList: 三种卡形态（父卡/叶卡/子卡）统一加 Switch（checked=enabled!==false，busy 时禁用）、批量勾选 Checkbox、已禁用琥珀徽章+正文降饱和+统计位 disabledCount；父卡头部点击展开，内嵌控件均 stopPropagation",
        "DocumentsTab: 编辑对话框扩为 text+answer+keywords 三字段（打开时从已载段落回填，保存仅成功即关），关键词客户端校验镜像 SegmentUpdateSchema（≤100 个、每个 ≤256 字符）；批量条（全选当前页/已选计数/启用/禁用，选择>500 内联提示+提交前拦截）",
        "部分成功语义：批量返回 updated<total 时 toast.warning 明示 {updated}/{total}，只有全成才 toast.success；单条开关失败弹错误 toast 且不吞状态",
        "testid 合同（e2e 稳定锚点）：segment-switch-{id}/segment-select-{id}/segment-edit-{id}/segment-delete-{id}/segment-batch-toggle/segment-batch-bar/segment-batch-count/segment-disabled-count；segment-edit 用 testid 是因为页头另有\"编辑\"菜单，getByRole 会误中",
        "切换文档/返回列表时清空段落选择与批量模式（selectDocument 收口）；新控件用合法 outline/default 变体（不碰 outline-solid 挂账）；Switch 默认尺寸即 24px 触控门槛",
        "i18n: knowledge.segment 新增 27 键双语，全部进 i18n:check"
      ]
    }
  ],
  "blockers": []
}
