# PCH-00 Baseline and Triage

Goal: turn static findings into a current, deduplicated implementation ledger.

Required evidence:

- Git branch/SHA and dirty-file boundary.
- Component, Web build and current Compose health.
- Simple TTFT/context and complex-task baseline.
- Baseline bundle manifest and selected hot-path call counts.
- Finding ledger that honors both reports' REJECTED and CORRECTED sections.

Exit: baseline commands are named, outputs recorded, and PCH-01 tests can be written without
guessing at current behavior.
