# PCH-04 Web and SDK

Scope: stream terminal identity, session epoch, render batching/block memoization, bundle/locale
splitting, and Java/Dart/Python SDK stream/image contracts.

Required gates:

- Web type-check, lint, build and i18n.
- Playwright long-stream, session-switch, cancel/error and route-load checks.
- Bundle budget comparison from generated manifest.
- SDK fixtures shaped exactly like production envelopes.
