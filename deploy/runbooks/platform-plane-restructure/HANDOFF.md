# Handoff

- Active phase: PPR-00
- Active feature: PPR-F001
- Status: **iteration 2 complete — local baseline CERTIFIED and FROZEN (run4 under G3/v2); resource profile EXECUTED, independently reviewed, and FROZEN (ppr00-stream-load-v1, 82/82); comprehensive whole-delta and merge-acceptance reviews DONE; all accepted fail-closed findings fixed. PPR-F001 PASSES and is authorized for commit/local merge — next: propose PPR-01 entry.**
- Completed (2026-08-28, iteration 2 — on top of the iteration-1 instrumentation bundle recorded below):
  - **Merge acceptance review** — fixed all-container Compose ownership enforcement, missing-memory-limit rejection, container-ID recreation detection, and bounded Docker CLI calls in the resource driver; repaired documentation index/link state and invalid stale-worktree cleanup guidance. Final PPR suite: 109 tests green; ruff clean; harness-check, test-isolation, sdk-sse-contract, assistant runtime regression, Agent Runtime release gate, validate, and status all pass. The running backend's accepted source hashes match the worktree and all 9 services are healthy.
  - **Four paid 107-trial reconciled replays** (receipts `reports/performance/assistant-ttft-baseline-2026-08-28-run{1..4}.json`, mode 0600, 4×107 trial calls + 10 probe trials, all successful):
    - run1 / run2 — both failed v1's per-trial `100 % reconciliation ok` rule on 3–4 trials whose client−server residual sat in the 0.200–0.228 s failure band (run2's distribution max 0.238 s is a warm-up trial (ordinal 1) that PASSED v1 via its relative `max(0.200, 0.05×client)` term on a 4.877 s client TTFT — tolerance 0.244 s). Run2 was executed on a deliberately quiet machine (run1 ran under host contention from parallel review agents) and reproduced run1's quantiles within ~8 ms through p99 → the tail is intrinsic out-of-window local work (auth/session/run-creation + Rust hop + transport + parse), **not** contention; identity violations 0, ceilings passed both runs.
    - **G3/v2 amendment** — user-authorized (option "quantified gate re-declaration", 2026-08-28): residual upper bound moved to `p99 ≤ 0.250 s` with a hard `0.500 s` defect ceiling; per-trial lower bound (`client ≥ server − 0.010`), identity-clean, and no-exclusion rules stay hard. Phase file keeps v1 verbatim marked SUPERSEDED with the full run-by-run disclosure. Fixed BEFORE run3.
    - run3 — v2 reconciliation PASSED; not recordable because a transient DashScope congestion burst hit the first 8 trials (`provider_wait` up to 45.7 s → client p95 6.42 s > ceiling). Reported honestly; user authorized an immediate re-run.
    - **run4 — CERTIFIED (`recordable: true`, exit 0)**: 107/107 successful, gate-set 105/105 individually `ok`, identity 0; its only `tolerance_exceeded` (0.24277 s) is warm-up ordinal 1 — informational under v2, disclosed for completeness.
  - **Local SLI gates frozen** (phase file "Frozen baseline", methodology note 4): `local_pre_provider_seconds` p95 **1.765 ms** (25 ms proposal supported ~14× headroom); client TTFT p95 **2.800 s** (p50 1.999, bootstrap CI95 [2.643, 3.533]); G3/v2 residual p99 **0.191 s** / max 0.243. `local_projection` excluded as gate input (pacing caveat). Provider pilot ≥30 raw: satisfied by run4's 105 gate trials, honestly labelled subset.
  - **Forced-delay probe**: 200 ms pre-dispatch inject via hot-update, 10 trials → landed entirely in `local_pre` (0.205–0.213), `provider_wait` unmoved, residuals unchanged; reverted; stack 9/9 healthy (`tmp/ppr00-probe-200ms.json`).
  - **Independent post-run reviews + fixes** (delta / security / server, parallel agents): single `recordable` certification bit (reader-less runs can never certify); all-trial reconciliation accounting; v2 quantile certification in `assistant_ttft_benchmark.py`; refuse-existing-output guard (A2); sentinel-credential non-appearance test (A5); cancelled-stream zero-timing-lines test (server F2); a variable-shadowing crash that would have lost an entire paid run's report on any missing receipt was caught by regression tests and fixed.
  - **Verification**: 24 benchmark + 23 timing + 35 model-plane tests pass; `make sdk-sse-contract` re-passed before freeze (G4); `make harness-check` OK; ruff clean on all touched files.
  - **Resource profile (`ppr00-stream-load-v1`, 2026-08-28)** — user-authorized single progressive run, levels 1,5,10,20, exactly 82/82 paid streams SUCCESS (zero admission rejects / 5xx / stream errors / timeouts / restarts / 90 %-mem-guard trips), exit 0 in 400.6 s; in-flight equalled K at every level. Driver `scripts/ppr00_resource_profile.py` (23 tests after post-run hardening, ruff clean; reuses `PprTtftAdapter` — verified effectively thread-safe for ≤20 concurrent unique-task streams). Report + raw samples (achieved cadence ≈ 2.03 s — 197 samples — not the requested 1 Hz; `docker stats --no-stream` costs ~1 s/cycle; true cadence re-derivable from `elapsed_seconds`) `reports/performance/ppr00-resource-2026-08-28{.json,.samples.jsonl}` (mode 0600); full record in phase file "Named concurrency/resource profile". **Independently reviewed (parallel agent) before freezing; the review's MAJOR narrative corrections were applied — its own initial robust-slope figures proved not reproducible, so every frozen number was re-derived from the raw samples, and the second full review pass verified all corrections by hand (TRUST)** (this is also why review-agent numbers are cross-checked, never copied). Frozen findings: steady-window Δ_K is a load-signal FLOOR; stream-path growth 3.33/1.06/3.34 (four-point fit) vs 3.43/1.14/3.80 (K≥5 robust, superlinear pg) MiB/stream for agent-runtime/backend/pg, redis+qdrant flat; level-end increments at K=20 +73.6/+22.8/+73.1 MiB; agent-runtime growth monotone-RETAINED across between-level settles (+9.2/+19.6/+38.6); first-event queueing tail only at K=20 (16 exceedances confined to ordinals 20–35, ≤0.98 s; 0 at K≤10). Live fix landed for pre-flight: agent-runtime health probe gained a container-local `docker exec` fallback (compose `expose`s 8094 with no host mapping — the original default would have aborted the entire paid run; fallback mirrors the compose healthcheck, regression-tested; reviewer's MINOR timeout-kwarg claim refuted against source — the original `_wait_for_health` never passed it either, 5 s default applies identically pre/post). **Comprehensive whole-delta final review 2026-08-28: evidence-chain audit COHERENT, profile-leg second pass TRUST, adversarial probe pass WHOLE-DELTA-OK (7/7 probes, 103 tests at review time); its three accepted driver fail-closed findings — mid-drain wall-clock re-check, zero-samples-cannot-certify, `"rate"` token substring misclassification ("generated"/"separate" → false admission-reject label) — were fixed post-run with regression tests + phase-file disclosure and CANNOT have touched the frozen artifact (400.6 s ≪ 90-min cap; 197 samples; 82/82 clean). Deferred server-side minors recorded as follow-ups to keep the verified 75/4 tree stable: dead `IDENTITY_TOLERANCE_SECONDS`, duplicated health-URL literals, fingerprint blind to dirty tree, no `ruff format --check` gate.**
- Disclosed gaps / caveats:
  - Per-trial token `usage` is null on the current stack — the shared adapter harvests usage from the `context_snapshot` streams the Rust cutover (4968068/fe2e1b8) removed from `assistant-turn-contract/v1`; the parity benchmark's snapshot-based `_validate_runtime_stream` is likewise dead on this stack (client now uses a receipt-model-enforcing `PprTtftAdapter`). Blocks PPR-F009 cost evidence; needs a usage-path fix or provider-console data.
  - Configuration fingerprint `ad7777a` predates the **uncommitted** implementation (instrumentation diff `model_plane.py` 66/4 at run4, now 75/4 after post-review server hardening — see phase methodology note 8; benchmark scripts + tests also uncommitted). Do not commit unless asked.
  - The ~0.17 s median client−server residual is itself a PPR-00 finding: out-of-window local work ≈ 8 % of TTFT at p50 — key input for PPR-03/PPR-04 topology decisions.
- Next actions:
  1. **Propose PPR-01 entry** (Rust build/distribution cheapness) to the user with the frozen PPR-00 baselines as the before-state; await go-ahead.
  2. The user authorized the complete acceptance bundle, including reproducibility drivers/tests and redacted performance receipts, for commit and local merge on 2026-08-28; no push was authorized.
- Blockers: none.
- Completed (2026-08-28, iteration 1, kept for audit):
  - **Instrument** — `ppr-timing/v1` additive schema (src/services/agent_runtime/timing.py): four gateway-process `perf_counter` stamps (stream_entry → dispatch → first_frame → first_visible), five components, first-write-wins, note-only (public contracts untouched). Stamped on both wire paths in `model_plane.stream()` / `_stream_native_responses()`; evidence seam is one credential-free log line per completed call (`Agent model-plane timing schema=ppr-timing/v1 …`), emitted strictly after call completion. Values render fixed 6-decimal (no scientific notation); native `t_first_visible` matches the re-encoded SSE event **type**, never payload substrings.
  - **Gates fixed before implementation** — G1–G5 + 8 methodology notes in the phase file (identity bound enforced per receipt, strict pass rule, single-stream provider_wait caveat, local gate definition under the paid channel, etc.).
  - **Tests** — `tests/services/agent_runtime/test_timing.py` (fake-clock exact identity, real-sleep wall-clock attribution within 50 ms bands, G2 isolation both paths, tool-argument non-stamp regression, G3 boundary matrix); benchmark parse/join/exponent/identity_violation/reader-containment/dedup-race tests. G4: unmodified `test_model_plane.py` + `make sdk-sse-contract` pass. Ruff clean.
  - **Reporting half** — `scripts/assistant_ttft_benchmark.py` v3 summary: per-trial reconciliation (sha256(run_id) join, status vocabulary, only `ok` counts under v1), G5 statistical policy (warm-up default 2, nearest-rank, IQR, seeded-0 bootstrap CIs), configuration fingerprint, ≤200 trials.
  - **Rollback bundle** — `rollback-bundle.json` digest-pinned at program-start commit `f884d1e` (reviewer verified all hashes).
  - **Stack** — local Docker stack hot-updated with the instrument; all 9 health checks green.
  - **Independent reviews (iteration 1)** — contract/correctness: 6 findings, all fixed, "safe to record"; methodology: accept-with-conditions, minimal conditions met.
  - **Audit stability** — gate text + methodology notes committed standalone as `ad7777a` on `rust-0828`.
  - **Credentials** — benchmark logs in through the project's own account path (`admin@AUTH_ALLOWED_EMAIL_DOMAIN` + `DEFAULT_USER_PASSWORD` bootstrap fallback), per the user's instruction; no separate ASSISTANT_TTFT_* credentials exist.
- Confirmation history (all 2026-08-28): paid high-sample replay channel; paid pilot ≥30; gate-text standalone commit (`ad7777a`); G3/v2 quantified amendment + run3; immediate run4 re-run; G3/v2 gate-text amendment standalone commit (`a082b4a`); resource-profile full 82-call single-progressive-run authorization (rejects as data, workers as-shipped).
- Decision: **PPR-F001 PASSES (2026-08-28).** All 4 steps + 7 Done-when items evidenced, frozen, and independently reviewed (contract, methodology, delta, security, server, profile-leg ×2, evidence-chain, whole-delta adversarial); comprehensive final review complete, post-run driver hardening disclosed. Next: propose PPR-01 entry.
