services:
  gateway:
    environment:
      AI_PLATFORM_TOPOLOGY_MODE: "compact"
  knowledge-service:
    environment:
      KNOWLEDGE_RUNTIME_ROLE: "all"
  knowledge-worker:
    profiles: [arc06-full-only]
