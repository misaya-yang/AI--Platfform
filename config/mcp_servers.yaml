# MCP Server Configuration
# Each entry defines an operator-managed remote system that the AI agent can use.

mcp_servers: []

# Built-in document generation is distributed as agent-plugins/ai-docgen and
# launched by assistant-service over stdio. Keep this file for platform-managed
# remote Streamable HTTP servers; tenant registrations remain HTTPS-only.
