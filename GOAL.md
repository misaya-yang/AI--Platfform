# GOAL 提示词

> 直接复制下面整段发给执行 agent。自包含，不依赖对话历史。

---

你在 `/Users/yang/projects/AI--Platfform` 工作。这是一个**企业级专业通用智能体**平台，核心是
`apps/assistant-service`（约 117k 行 Python）。

## 目标

对标 Hermes Agent 与 OpenClaw，**找到所有缺陷 → 升级优化 → 测试回归**。

对标对象在本机有完整源码，直接读，不要用 README 或版本说明代替读源码：

- Hermes Agent：`/Users/yang/projects/Hermes_agent`
- OpenClaw：`/Users/yang/projects/open claw/openclaw`

**"缺陷"不限于 bug。能力缺口也是缺陷**——对标对象能完成而我们完不成的任务，就是缺陷。

差异化定位，一句话：**通用能力追平对标对象，企业属性领先对标对象。**

- 追平方向：环境控制、人机协同、多智能体、扩展生态——这是当前差距的主要来源
- 领先方向：多租户隔离、审批网关、KB 数据集权限、租户级模型路由、可审计持久事件——对标对象
  是单用户本地优先架构，天然不具备这些

判定是否跑偏：如果某个改动让平台更像"一个更好的单机 agent"而不是"一个企业能部署的通用
agent"，就是跑偏了。

## 怎么算对标（这是硬要求）

数工具个数、比文件数量**不算对标**。必须三层同时做，缺一层结论不成立：

1. **任务级（最高权重）**——选 8–12 个真实企业任务，覆盖多步推理、外部检索、文件产出、歧义
   澄清、长时任务、失败恢复。三个系统各跑一遍，比最终交付物。判定必须是**结果级**：交付物是否
   真的满足任务要求。**"非空输出"不计为完成。** 记录完成率、步数、耗时、token，失败的写明卡在
   哪一步、什么原因。
2. **能力面**——对标对象具备的每一项能力，落到三选一，不许留空：**已覆盖**（写出我们的实现
   位置）／**采纳**（写出为什么值得做、落到哪个里程碑）／**不采纳**（写出理由）。合法的不采纳
   理由：与多租户模型冲突、与审批隔离边界冲突、目标用户不需要。**"没时间"不是理由**，那属于
   "采纳但排后"。
3. **架构取舍**——分清哪些差距是"我们该补的"，哪些是"选了多租户而非单用户本地优先的必然
   代价"。把后者误当缺陷去补，会破坏企业级定位。

产出：`reports/benchmark/agent-capability-gap-YYYY-MM-DD.md`，含任务级结果表（附原始 trace 或
SSE artifact 引用）、能力差距矩阵、架构取舍说明、由矩阵推导的优先级排序。

## 验收标准

五条，全部可机器执行或可复现判定。任一不满足即未达成。

- **A1 任务完成率 ≥ 对标对象**，结果级判定，低于的每项写明原因
- **A2 能力矩阵 0 项未决**
- **A3 运行时可信三条**：
  - **不失控**——构造重复调用同一工具的场景，`LoopDetection` 实际拦截；构造超时场景，
    `TimeBudget` 实际拦截。均在**默认**中间件链生效，无需额外配置
  - **能诊断**——任意内部异常在服务端日志可读到脱敏后的 message 与 traceback；SSE 与 trace 的
    用户可见字段**仍然**只有脱敏摘要
  - **配置可知**——启动时输出"已解析开关指纹"（每开关最终生效值 + 来源）并纳入 trace；密钥类
    只输出是否已设置
- **A4 回归全绿**：`.venv/bin/python -m pytest tests/ -q --no-header --no-cov --ignore=tests/integration`
  0 失败
- **A5 每条结论可复现**：任何"已完成/已验证"必须附执行的命令、通过/失败/跳过计数、每个失败项
  的归因（既有缺陷／环境问题／本次引入）。不满足的视为未验证

## 边界（不得越过）

**定位**

- 不得为了追平对标对象而牺牲企业级定位。单用户本地优先架构的便利，在多租户环境下往往是越权。
  "全主机信任""全局 token""sandbox 默认关闭"这类设计一律拒绝
- 不得放宽既有的审批、租户隔离、能力白名单、脱敏行为。确需变更则单独立项

**架构**

- 不得新建第二条执行路径。canonical AgentLoop / ExecutionGateway / CapabilityAllowlist 是唯一
  执行面；Local Node、OpenAI Responses 只能是其 adapter
- 不得修改 `turn_event_collector` 对 terminal envelope 的逐字节一致性校验

**过程**

- 不得为了让测试变绿而改测试断言。冲突时先判定哪边对，理由写进提交信息
- 不得记录未实际运行的验证
- 声称修复前，先有一个能复现该问题的失败测试。没红过的测试，不能证明它变绿是因为你的改动
- 每项改动可独立回滚

**协作（本仓库有多个 agent 并发编辑）**

- 状态还原只用 `git`，**不要用文件拷贝做快照**。曾因 `cp` 还原一个正被并发编辑的文件而覆盖对方
  改动，凭空产生 81 个失败并被误判为代码缺陷
- 改他人正在编辑的文件时，压到最小的单个 hunk

**明确不做**（避免反复被当成紧急项）

- 评测器抗造假加固——回执伪造在本项目威胁模型里不成立，没有外部方提交回执。该问的是"评测
  分数能不能预测真实任务表现"，那是有效性问题不是防伪问题
- 工具发现策略调整——在拿到"往返成本 vs 上下文成本"实测数据前不要改

## 起点

先读 `AGENTS.md` 和 `deploy/runbooks/assistant-runtime-trust-hardening.md`（明细计划与已确认
的运行时欠账，含具体文件行号）。

当前基线 `5983 passed, 1 failed`，唯一失败是 `attachment.deleted_at`——`agent_repository.py:5314`
与 `:7370` 查询该列，但 `database/migrations/080_...sql:24` 建表时没有它，全库无迁移创建。这是
main 上的既有缺陷，修法需先定语义（会改变租户配额计算）。

不得 commit、push、部署、迁移或清理容器，除非明确授权。
